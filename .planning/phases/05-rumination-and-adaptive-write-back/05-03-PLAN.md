---
phase: 05-rumination-and-adaptive-write-back
plan: 03
type: execute
wave: 3
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - src/cognition/rumination.rs
  - src/memory/repository.rs
  - tests/rumination_governance_integration.rs
autonomous: true
requirements:
  - LRN-03
must_haves:
  truths:
    - "Long-cycle rumination can turn accumulated evidence into `skill_template`, `promotion_candidate`, and `value_adjustment_candidate` outputs."
    - "All long-cycle outputs share one candidate/item contract even though they originate from explicit `LPQ` work."
    - "Shared-truth-facing outputs remain proposal-driven and appear in governance queues rather than being auto-approved or applied directly."
  artifacts:
    - path: "src/cognition/rumination.rs"
      provides: "LPQ candidate generation and governance bridging service"
      exports: ["LongCycleCandidateReport", "RuminationCandidateKind"]
    - path: "src/memory/repository.rs"
      provides: "Unified rumination candidate persistence and lookup methods"
    - path: "tests/rumination_governance_integration.rs"
      provides: "Regression coverage for long-cycle candidate generation and governance compatibility"
  key_links:
    - from: "src/cognition/rumination.rs"
      to: "src/memory/repository.rs"
      via: "persist unified long-cycle candidates with governance refs and evidence"
      pattern: "rumination_candidates"
    - from: "src/cognition/rumination.rs"
      to: "src/memory/governance.rs"
      via: "promotion candidates materialize through TruthGovernanceService instead of direct shared-truth writes"
      pattern: "create_promotion_review|create_ontology_candidate"
    - from: "src/cognition/rumination.rs"
      to: "src/agent/orchestration.rs"
      via: "LPQ synthesis consumes prior reports and citations instead of re-running retrieval"
      pattern: "AgentSearchReport"
---

<objective>
Implement long-cycle `LPQ` write-back so accumulated evidence becomes auditable skill, governance, and value-adjustment candidates.

Purpose: Complete the learning loop without collapsing governance boundaries or turning LPQ into automatic truth mutation.
Output: Unified long-cycle candidate generation, candidate persistence, and governance-queue integration tests.
</objective>

<execution_context>
@/home/tongyuan/.codex/get-shit-done/workflows/execute-plan.md
@/home/tongyuan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-rumination-and-adaptive-write-back/05-CONTEXT.md
@.planning/phases/05-rumination-and-adaptive-write-back/05-RESEARCH.md
@.planning/phases/05-rumination-and-adaptive-write-back/05-PATTERNS.md
@.planning/phases/03-truth-layer-governance/03-02-SUMMARY.md
@.planning/phases/03-truth-layer-governance/03-03-SUMMARY.md
@.planning/phases/04-working-memory-and-agent-search/04-03-SUMMARY.md
@src/cognition/rumination.rs
@src/memory/governance.rs
@src/memory/repository.rs
@src/agent/orchestration.rs
@src/cognition/report.rs

<interfaces>
Existing governance APIs to reuse:
```rust
pub struct TruthGovernanceService<'db> {
    pub fn create_promotion_review(
        &self,
        request: CreatePromotionReviewRequest,
    ) -> Result<PromotionReviewReport, TruthGovernanceError>;

    pub fn attach_evidence(
        &self,
        request: AttachPromotionEvidenceRequest,
    ) -> Result<PromotionReviewReport, TruthGovernanceError>;

    pub fn create_ontology_candidate(
        &self,
        request: CreateOntologyCandidateRequest,
    ) -> Result<OntologyCandidate, TruthGovernanceError>;
}
```

Existing LPQ evidence input seam:
```rust
pub struct AgentSearchReport {
    pub decision: DecisionReport,
    pub retrieval_steps: Vec<RetrievalStepReport>,
    pub citations: Vec<Citation>,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement unified LPQ candidate generation for the three long-cycle output classes</name>
  <files>src/cognition/rumination.rs, src/memory/repository.rs, tests/rumination_governance_integration.rs</files>
  <behavior>
    - Ready LPQ items generate only `skill_template`, `promotion_candidate`, or `value_adjustment_candidate`.
    - All three outputs share one durable candidate contract with source queue item ID, evidence refs, payload JSON, and candidate state.
    - LPQ processing remains bounded and opportunity-driven; it does not run as a daemon and does not auto-apply any candidate.
  </behavior>
  <action>Per D-09, D-10, and D-11, extend `src/cognition/rumination.rs` and `src/memory/repository.rs` so LPQ drain creates one unified `RuminationCandidate` contract backed by the `rumination_candidates` side table added in 05-01. Candidate synthesis must consume existing Phase 4 reports/citations and previously persisted queue payloads instead of re-running retrieval or rebuilding working memory. Keep `skill_template` strictly as a candidate-first durable row in `rumination_candidates`, with no execution engine, retrieval interface, or separate skill subsystem added in this phase. Write failing tests first in `tests/rumination_governance_integration.rs` to prove the three output classes exist, preserve evidence links, and stay pending/candidate-first.</action>
  <verify>
    <automated>rtk cargo test --test rumination_governance_integration lpq_generates_unified_candidates_from_accumulated_evidence -- --nocapture</automated>
  </verify>
  <done>LPQ processing emits auditable `skill_template`, `promotion_candidate`, and `value_adjustment_candidate` rows through one shared candidate schema.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bridge shared-truth-facing LPQ outputs through Phase 3 governance seams</name>
  <files>src/cognition/rumination.rs, src/memory/repository.rs, tests/rumination_governance_integration.rs</files>
  <behavior>
    - `promotion_candidate` paths materialize canonical governance objects through `TruthGovernanceService` and store the resulting reference IDs.
    - No LPQ path auto-approves promotion reviews or ontology candidates, and no code writes shared T1/T2 authority rows directly.
    - `skill_template` and `value_adjustment_candidate` stay inside the rumination candidate store for later review/consumption.
  </behavior>
  <action>Per D-11 through D-14, wire long-cycle shared-truth-facing outputs to existing Phase 3 governance APIs instead of introducing a second proposal lifecycle. For T3-to-T2 style outputs, create pending promotion reviews and attach evidence; for T2-to-T1 style outputs, create ontology candidates; in both cases persist the canonical `governance_ref_id` back onto the `promotion_candidate` rumination row. Add integration tests proving Phase 3 pending review/candidate queues become visible, no approval state is auto-set, and long-cycle processing does not re-implement retrieval, value scoring, or Rig orchestration.</action>
  <verify>
    <automated>rtk cargo test --test rumination_governance_integration lpq_bridges_to_governance_without_auto_approval -- --nocapture</automated>
  </verify>
  <done>Long-cycle promotion outputs feed the existing governance queues as pending proposals, while skill/value candidates remain proposal-first and no shared truth is auto-mutated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LPQ processor -> rumination candidate store | Slow-cycle learning crosses from accumulated evidence into durable proposal state here. |
| rumination candidate store -> Phase 3 governance service | Shared-truth-facing candidates are handed to canonical governance queues here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | E | `src/cognition/rumination.rs` | mitigate | Route all shared-truth-facing LPQ outputs through `TruthGovernanceService` and forbid direct T1/T2 authority writes so LPQ cannot self-approve changes. |
| T-05-08 | T | `src/memory/repository.rs` | mitigate | Persist candidate kind, source queue item ID, evidence refs, and canonical governance ref IDs so proposal state cannot drift into an opaque second governance system. |
| T-05-09 | R | `tests/rumination_governance_integration.rs` | mitigate | Cover pending review/candidate visibility and non-approval behavior so long-cycle outputs remain auditable and clearly distinct from approved truth. |
</threat_model>

<verification>
- `rtk cargo test --test rumination_governance_integration -- --nocapture`
- `rtk cargo test --test truth_governance -- --nocapture`
- `rtk cargo clippy --all-targets -- -D warnings`
</verification>

<success_criteria>
- LPQ emits the three required long-cycle output classes under one candidate contract with evidence and queue-item lineage.
- Shared-truth-facing outputs appear in Phase 3 governance queues as pending proposals and never auto-approve themselves.
- Long-cycle implementation consumes existing reports/governance seams and does not re-implement retrieval, working memory, value scoring, or Rig orchestration.
</success_criteria>

<output>
After completion, create `.planning/phases/05-rumination-and-adaptive-write-back/05-03-SUMMARY.md`
</output>
