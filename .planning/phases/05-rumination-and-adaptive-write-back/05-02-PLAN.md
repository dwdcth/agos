---
phase: 05-rumination-and-adaptive-write-back
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - src/cognition/assembly.rs
  - src/cognition/rumination.rs
  - src/memory/repository.rs
  - tests/rumination_writeback.rs
autonomous: true
requirements:
  - LRN-02
must_haves:
  truths:
    - "Short-cycle rumination can turn failures, user corrections, and metacognitive vetoes into local adaptive updates for the next step."
    - "Short-cycle write-back updates `self_state`, `risk_boundary`, and local/private T3-adjacent adaptation state only."
    - "Short-cycle processing never mutates shared T2/T1 truth or auto-approves governance state."
  artifacts:
    - path: "src/cognition/rumination.rs"
      provides: "Short-cycle SPQ processor and write-back reports"
      exports: ["ShortCycleWritebackReport"]
    - path: "src/cognition/assembly.rs"
      provides: "Self-state overlay/provider composition that reads local adaptation entries without changing working-memory immutability"
    - path: "src/memory/repository.rs"
      provides: "Local adaptation ledger reads and writes keyed to rumination queue items"
    - path: "tests/rumination_writeback.rs"
      provides: "Regression coverage for local write-back boundaries and non-mutation of shared truth"
  key_links:
    - from: "src/cognition/rumination.rs"
      to: "src/memory/repository.rs"
      via: "persist local adaptation entries from claimed SPQ items"
      pattern: "local_adaptation"
    - from: "src/cognition/assembly.rs"
      to: "src/memory/repository.rs"
      via: "overlay provider reads active local adaptation entries into self_state"
      pattern: "SelfStateProvider"
    - from: "src/cognition/rumination.rs"
      to: "src/memory/governance.rs"
      via: "no write path to shared-truth governance APIs during short-cycle processing"
      pattern: "TruthGovernanceService"
---

<objective>
Implement short-cycle write-back on top of `SPQ` so immediate corrective learning updates only local adaptive state.

Purpose: Fulfill the controlled short-cycle learning requirement without corrupting shared truth or bypassing Phase 3 governance seams.
Output: SPQ write-back logic, a durable local adaptation ledger, and tests proving the write-back boundary.
</objective>

<execution_context>
@/home/tongyuan/.codex/get-shit-done/workflows/execute-plan.md
@/home/tongyuan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-rumination-and-adaptive-write-back/05-CONTEXT.md
@.planning/phases/05-rumination-and-adaptive-write-back/05-RESEARCH.md
@.planning/phases/05-rumination-and-adaptive-write-back/05-PATTERNS.md
@.planning/phases/04-working-memory-and-agent-search/04-01-SUMMARY.md
@.planning/phases/04-working-memory-and-agent-search/04-02-SUMMARY.md
@src/cognition/assembly.rs
@src/cognition/working_memory.rs
@src/cognition/report.rs
@src/cognition/rumination.rs
@src/memory/repository.rs
@src/memory/governance.rs

<interfaces>
Existing self-state seam:
```rust
pub trait SelfStateProvider {
    fn snapshot(&self, request: &WorkingMemoryRequest, truths: &[TruthRecord]) -> SelfStateSnapshot;
}

pub struct SelfStateSnapshot {
    pub task_context: Option<String>,
    pub capability_flags: Vec<String>,
    pub readiness_flags: Vec<String>,
    pub facts: Vec<SelfStateFact>,
}
```

Existing short-cycle input seam from Phase 4:
```rust
pub struct DecisionReport {
    pub gate: GateReport,
    pub active_risks: Vec<String>,
    pub metacog_flags: Vec<MetacognitiveFlag>,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the local adaptation ledger and self-state overlay seam</name>
  <files>src/cognition/assembly.rs, src/memory/repository.rs, tests/rumination_writeback.rs</files>
  <behavior>
    - Local adaptation entries are typed to `self_state`, `risk_boundary`, or local/private `t3_adaptation`, each linked back to a source queue item and evidence refs.
    - Working-memory assembly can read those entries through a provider/overlay seam without making `WorkingMemory` durable or mutable.
    - Shared truth tables remain unchanged; the overlay sits beside existing truth projections rather than replacing them.
  </behavior>
  <action>Per D-06, D-07, and D-08, add repository methods and DTOs for inserting and listing `local_adaptation_entries`, then extend `src/cognition/assembly.rs` with an overlay-style provider composition that merges current request-local state with active adaptation entries. Keep the Phase 4 pattern intact: `WorkingMemory` stays runtime-only and immutable, while `SelfStateProvider` remains the integration seam. Write tests first in `tests/rumination_writeback.rs` proving adaptive self/risk facts appear in snapshots and that no shared-truth rows are modified as part of this overlay path.</action>
  <verify>
    <automated>rtk cargo test --test rumination_writeback short_cycle_overlay_provider_reads_local_adaptations -- --nocapture</automated>
  </verify>
  <done>Local adaptation entries are queryable, self-state assembly can consume them through a provider seam, and the overlay path leaves shared truth untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement SPQ short-cycle write-back with strict local-only boundaries</name>
  <files>src/cognition/rumination.rs, src/memory/repository.rs, tests/rumination_writeback.rs</files>
  <behavior>
    - Claimed SPQ items from action failure, user correction, and metacognitive veto create local adaptation entries and completion reports.
    - The processor may update only `self_state`, `risk_boundary`, and local/private T3-adjacent adaptation state.
    - No code path updates shared T2/T1 truth rows, promotion reviews, or ontology candidates during short-cycle drain.
  </behavior>
  <action>Implement the short-cycle processor in `src/cognition/rumination.rs` on top of the 05-01 queue foundation. Per D-06 through D-08 and D-12 through D-14, derive corrective local adaptations from `DecisionReport`, `AgentSearchReport`, explicit action-failure envelopes, and user-correction events; persist them through `src/memory/repository.rs`; and mark SPQ items as completed or retriable with audit metadata. Add regression tests proving short-cycle processing writes only to the local adaptation ledger, respects the queue throttle rules from 05-01, and never mutates `memory_records`, `truth_promotion_reviews`, or `truth_ontology_candidates` directly.</action>
  <verify>
    <automated>rtk cargo test --test rumination_writeback short_cycle_writeback_updates_local_state_without_mutating_shared_truth -- --nocapture</automated>
  </verify>
  <done>Short-cycle rumination drains ready SPQ items into local adaptive state, improves the next-step self/risk picture, and preserves the governance boundary around shared truth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SPQ processor -> local adaptation ledger | Immediate corrective learning crosses from transient reports into durable local adaptive state here. |
| local adaptation ledger -> self-state assembly | Durable local adaptations are read back into future working-memory snapshots here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | E | `src/cognition/rumination.rs` | mitigate | Restrict short-cycle writes to `local_adaptation_entries` and explicitly forbid writes to `memory_records`, promotion approvals, or ontology approvals in tests and service code. |
| T-05-05 | T | `src/cognition/assembly.rs` | mitigate | Compose the adaptive overlay through `SelfStateProvider` so local facts augment snapshots without replacing truth projections or mutating immutable `WorkingMemory` values. |
| T-05-06 | R | `src/memory/repository.rs` | mitigate | Record source queue item IDs, trigger kinds, and evidence refs with every local adaptation entry so corrective changes remain auditable and reversible. |
</threat_model>

<verification>
- `rtk cargo test --test rumination_writeback -- --nocapture`
- `rtk cargo test --test working_memory_assembly -- --nocapture`
- `rtk cargo clippy --all-targets -- -D warnings`
</verification>

<success_criteria>
- SPQ short-cycle processing writes only local adaptive updates for self/risk/private T3-adjacent state.
- Future self-state snapshots can read those updates through the existing assembly seam without changing WorkingMemory immutability.
- No shared T2/T1 truth mutation or governance auto-approval occurs anywhere in the short-cycle path.
</success_criteria>

<output>
After completion, create `.planning/phases/05-rumination-and-adaptive-write-back/05-02-SUMMARY.md`
</output>
