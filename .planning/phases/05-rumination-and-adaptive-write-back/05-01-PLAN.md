---
phase: 05-rumination-and-adaptive-write-back
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/0005_rumination_writeback.sql
  - src/core/migrations.rs
  - src/cognition/mod.rs
  - src/cognition/rumination.rs
  - src/memory/repository.rs
  - tests/foundation_schema.rs
  - tests/rumination_queue.rs
autonomous: true
requirements:
  - LRN-01
must_haves:
  truths:
    - "System persists explicit `SPQ` and `LPQ` work items instead of mixing all learning into one undifferentiated batch."
    - "Action failure, user correction, and metacognitive veto enter `SPQ`, while session/evidence/idle/abnormal-pattern triggers enter `LPQ`."
    - "`SPQ` is always claimed ahead of `LPQ`, and repeated triggers are throttled by deduplication, cooldown, and bounded budget rules."
  artifacts:
    - path: "migrations/0005_rumination_writeback.sql"
      provides: "Additive rumination queue, throttle, local adaptation, and candidate side tables"
    - path: "src/cognition/rumination.rs"
      provides: "Typed rumination trigger normalization and queue scheduling service"
      exports: ["RuminationTriggerEvent", "RuminationQueueTier", "RuminationService"]
    - path: "src/memory/repository.rs"
      provides: "Durable queue and trigger-state persistence methods for explicit SPQ/LPQ scheduling"
    - path: "tests/rumination_queue.rs"
      provides: "Regression coverage for routing, dedupe, cooldown, budget, and SPQ-over-LPQ priority"
  key_links:
    - from: "src/cognition/rumination.rs"
      to: "src/memory/repository.rs"
      via: "queue persistence and claim/update methods"
      pattern: "MemoryRepository"
    - from: "src/cognition/rumination.rs"
      to: "src/cognition/report.rs"
      via: "DecisionReport and gate outcomes inform trigger routing"
      pattern: "DecisionReport"
    - from: "src/cognition/rumination.rs"
      to: "src/agent/orchestration.rs"
      via: "AgentSearchReport provides cited evidence for queue payloads"
      pattern: "AgentSearchReport"
---

<objective>
Build the durable rumination control-plane foundation: explicit `SPQ`/`LPQ` queues, trigger normalization, throttling, and bounded scheduling.

Purpose: Satisfy the routing requirement before any write-back logic lands, while preserving the existing retrieval, working-memory, value, and Rig boundaries.
Output: A Phase 5 migration, repository queue methods, a new `cognition::rumination` seam, and queue scheduling tests.
</objective>

<execution_context>
@/home/tongyuan/.codex/get-shit-done/workflows/execute-plan.md
@/home/tongyuan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-rumination-and-adaptive-write-back/05-CONTEXT.md
@.planning/phases/05-rumination-and-adaptive-write-back/05-RESEARCH.md
@.planning/phases/05-rumination-and-adaptive-write-back/05-PATTERNS.md
@.planning/phases/03-truth-layer-governance/03-03-SUMMARY.md
@.planning/phases/04-working-memory-and-agent-search/04-02-SUMMARY.md
@.planning/phases/04-working-memory-and-agent-search/04-03-SUMMARY.md
@src/cognition/report.rs
@src/agent/orchestration.rs
@src/memory/repository.rs
@src/core/migrations.rs

<interfaces>
Existing Phase 4 report seams:
```rust
pub struct DecisionReport {
    pub scored_branches: Vec<ScoredBranchReport>,
    pub selected_branch: Option<ScoredBranchReport>,
    pub gate: GateReport,
    pub active_risks: Vec<String>,
    pub metacog_flags: Vec<MetacognitiveFlag>,
}

pub struct AgentSearchReport {
    pub working_memory: WorkingMemory,
    pub decision: DecisionReport,
    pub retrieval_steps: Vec<RetrievalStepReport>,
    pub citations: Vec<Citation>,
    pub executed_steps: usize,
    pub step_limit: usize,
}
```

Existing repository/governance seam to preserve:
```rust
pub struct MemoryRepository<'db> {
    pub fn new(conn: &'db Connection) -> Self;
}

pub struct TruthGovernanceService<'db> {
    pub fn new(conn: &'db Connection) -> Self;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the Phase 5 durable queue and throttle schema</name>
  <files>migrations/0005_rumination_writeback.sql, src/core/migrations.rs, src/cognition/mod.rs, tests/foundation_schema.rs, tests/rumination_queue.rs</files>
  <behavior>
    - Schema exposes explicit durable `SPQ` and `LPQ` queues, not one mixed-priority learning batch.
    - Queue storage preserves a shared queue-item contract across both queues plus durable trigger throttle state for dedupe, cooldown, and budget tracking.
    - Migration is additive and does not alter `memory_records`, FTS, truth-governance authority rows, or Rig boundaries.
  </behavior>
  <action>Per D-01, D-03, D-04, and D-05, create `0005_rumination_writeback.sql` with explicit `spq_queue_items` and `lpq_queue_items` tables that mirror the same queue-item columns, plus `rumination_trigger_state`, `local_adaptation_entries`, and `rumination_candidates` side tables needed by later plans. Register the migration in `src/core/migrations.rs`, export `pub mod rumination;` from `src/cognition/mod.rs`, and extend schema tests so version 5 and the new rumination side tables are enforced. In `tests/rumination_queue.rs`, write the failing queue-focused tests first so later implementation must prove explicit dual queues, throttle durability, and additive schema compatibility.</action>
  <verify>
    <automated>rtk cargo test --test foundation_schema rumination_schema_bootstraps_version_5_side_tables -- --nocapture</automated>
  </verify>
  <done>Schema version 5 exists, Phase 5 side tables are additive, and test scaffolding asserts explicit SPQ/LPQ storage plus throttle ledgers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement trigger normalization and bounded SPQ/LPQ scheduling</name>
  <files>src/cognition/rumination.rs, src/memory/repository.rs, tests/rumination_queue.rs</files>
  <behavior>
    - `action_failure`, `user_correction`, and metacognitive veto route to `SPQ`; `session_boundary`, `evidence_accumulation`, `idle_window`, and `abnormal_pattern_accumulation` route to `LPQ`.
    - Trigger processing follows `route -> dedupe -> cooldown -> budget -> enqueue`, and `SPQ` claim order always wins over ready `LPQ` work.
    - The scheduler consumes existing Phase 4 report DTOs as inputs and does not re-implement retrieval, working memory, value scoring, or Rig orchestration.
  </behavior>
  <action>Implement `src/cognition/rumination.rs` as the Phase 5 control-plane seam with typed `RuminationTriggerEvent`, `RuminationQueueTier`, `RuminationQueueItem`, `RuminationTriggerDecision`, and `RuminationService` contracts. Per D-01 through D-05, add repository methods in `src/memory/repository.rs` for enqueueing, claiming, completing, and retrying explicit SPQ/LPQ items plus reading/updating durable trigger-state ledgers. Normalize inputs from existing `DecisionReport` and `AgentSearchReport` into auditable queue payloads, keep scheduling synchronous/local-first, and prove with tests that dedupe, cooldown, budget caps, and SPQ-over-LPQ priority work without introducing a daemon or any truth-write path.</action>
  <verify>
    <automated>rtk cargo test --test rumination_queue -- --nocapture</automated>
  </verify>
  <done>The codebase has an auditable rumination queue scheduler that enqueues the right triggers into explicit SPQ/LPQ queues, throttles repeats durably, and always prefers SPQ when both queues are ready.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 4 reports -> rumination scheduler | Action and metacognitive outcomes cross into durable learning control-plane state here. |
| rumination scheduler -> SQLite queue tables | Queue priority, throttle ledgers, and retry state persist across commands here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | D | `src/cognition/rumination.rs` | mitigate | Persist dedupe, cooldown, and budget ledgers in `rumination_trigger_state` so repeated failures or vetoes cannot cause trigger storms across commands. |
| T-05-02 | T | `src/memory/repository.rs` | mitigate | Use explicit SPQ/LPQ queue tables with checked status enums and deterministic claim ordering so LPQ work cannot silently overtake SPQ safety work. |
| T-05-03 | R | `src/cognition/rumination.rs` | mitigate | Store source report refs, evidence refs, and queue-item audit metadata in payload JSON so routing decisions remain explainable and replayable. |
</threat_model>

<verification>
- `rtk cargo test --test foundation_schema rumination_schema_bootstraps_version_5_side_tables -- --nocapture`
- `rtk cargo test --test rumination_queue -- --nocapture`
- `rtk cargo clippy --all-targets -- -D warnings`
</verification>

<success_criteria>
- Phase 5 has explicit durable `SPQ` and `LPQ` queues with shared item shape and auditable trigger-state throttling.
- All locked trigger classes route to the correct queue and are bounded by dedupe, cooldown, and budget enforcement.
- `SPQ` is always scheduled ahead of `LPQ`, and none of the new code reimplements or bypasses retrieval, working memory, value scoring, or Rig orchestration.
</success_criteria>

<output>
After completion, create `.planning/phases/05-rumination-and-adaptive-write-back/05-01-SUMMARY.md`
</output>
