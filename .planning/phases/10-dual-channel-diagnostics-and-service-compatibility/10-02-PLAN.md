---
phase: 10-dual-channel-diagnostics-and-service-compatibility
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/agent/orchestration.rs
  - src/interfaces/cli.rs
  - tests/agent_search.rs
autonomous: true
requirements:
  - OPS-03
must_haves:
  truths:
    - "Agent-search continues to consume ordinary retrieval services under dual-channel mode selection."
    - "No semantic-only bypass path is introduced for agent-search."
    - "AgentSearchReport remains compatible with the enriched ordinary retrieval contract."
  artifacts:
    - path: "src/agent/orchestration.rs"
      provides: "agent-search compatibility over ordinary retrieval mode selection"
    - path: "tests/agent_search.rs"
      provides: "regression coverage that agent-search still reuses ordinary retrieval under dual-channel modes"
  key_links:
    - from: "src/agent/orchestration.rs"
      to: "src/search/mod.rs"
      via: "ordinary retrieval seam reused under explicit mode selection"
      pattern: "SearchRequest / SearchResponse"
    - from: "tests/agent_search.rs"
      to: "src/agent/orchestration.rs"
      via: "compatibility assertions for dual-channel ordinary retrieval reuse"
      pattern: "AgentSearchOrchestrator"
---

<objective>
Guarantee that higher layers, especially agent-search, keep reusing ordinary dual-channel retrieval services instead of drifting into a semantic-only compatibility break.

Purpose: close the final v1.1 compatibility seam between dual-channel retrieval and agent-search.
Output: mode-aware agent-search compatibility plus regression coverage that the shared retrieval seam remains intact.
</objective>

<execution_context>
@/home/tongyuan/.codex/get-shit-done/workflows/execute-plan.md
@/home/tongyuan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-dual-channel-diagnostics-and-service-compatibility/10-CONTEXT.md
@.planning/phases/10-dual-channel-diagnostics-and-service-compatibility/10-RESEARCH.md
@.planning/phases/10-dual-channel-diagnostics-and-service-compatibility/10-PATTERNS.md
@src/agent/orchestration.rs
@src/interfaces/cli.rs
@src/search/mod.rs
@tests/agent_search.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add agent-search compatibility regressions for dual-channel mode selection</name>
  <files>tests/agent_search.rs</files>
  <read_first>
    - tests/agent_search.rs
    - src/agent/orchestration.rs
    - src/interfaces/cli.rs
    - src/search/mod.rs
  </read_first>
  <acceptance_criteria>
    - `tests/agent_search.rs` defines `agent_search_reuses_ordinary_retrieval_under_dual_channel_modes`
    - `tests/agent_search.rs` defines `dual_channel_mode_selection_preserves_agent_report_contract`
    - `cargo test --test agent_search agent_search_reuses_ordinary_retrieval_under_dual_channel_modes -- --nocapture` fails before production changes and passes after Task 2
  </acceptance_criteria>
  <behavior>
    - Agent-search must keep using ordinary retrieval.
    - Dual-channel mode selection must not introduce a semantic-only bypass path.
    - AgentSearchReport must remain compatible with enriched retrieval traces and mode selection.
  </behavior>
  <action>Extend `tests/agent_search.rs` with `agent_search_reuses_ordinary_retrieval_under_dual_channel_modes`, asserting that agent-search still invokes the ordinary retrieval seam under explicit dual-channel mode selection, and `dual_channel_mode_selection_preserves_agent_report_contract`, asserting that the resulting `AgentSearchReport` remains structured, cited, and compatible with the enriched ordinary retrieval trace contract. Keep the tests focused on seam reuse and report compatibility, not on rebuilding retrieval behavior already covered in Phase 9.</action>
  <verify>
    <automated>cargo test --test agent_search agent_search_reuses_ordinary_retrieval_under_dual_channel_modes -- --nocapture</automated>
  </verify>
  <done>A failing compatibility regression harness defines the final invariant that agent-search must continue to reuse ordinary retrieval after dual-channel mode control lands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread dual-channel mode selection through agent-search without adding a bypass path</name>
  <files>src/agent/orchestration.rs, src/interfaces/cli.rs, tests/agent_search.rs</files>
  <read_first>
    - src/agent/orchestration.rs
    - src/interfaces/cli.rs
    - src/search/mod.rs
    - tests/agent_search.rs
    - .planning/phases/10-dual-channel-diagnostics-and-service-compatibility/10-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - agent-search can use explicit dual-channel mode selection while still consuming ordinary retrieval services
    - no new semantic-only retrieval path is added for agent-search
    - `cargo test --test agent_search -- --nocapture` exits 0
    - `cargo clippy --all-targets -- -D warnings` exits 0
  </acceptance_criteria>
  <behavior>
    - Agent-search remains a consumer, not a special retrieval provider.
    - Report/citation structure remains intact after mode selection plumbing.
    - Phase 10 still avoids new interface surfaces beyond existing CLI/library seams.
  </behavior>
  <action>In `src/agent/orchestration.rs` and any necessary CLI entrypoint code, add the minimal mode-selection plumbing needed so agent-search can continue to call ordinary retrieval under lexical-only, embedding-only, or hybrid mode selection. Do this by threading the selected mode through the existing ordinary retrieval seam, not by adding a semantic-only retrieval path or separate semantic agent logic. Update the new agent-search compatibility regressions so they prove ordinary retrieval reuse and report-contract stability under dual-channel mode selection.</action>
  <verify>
    <automated>cargo test --test agent_search -- --nocapture</automated>
  </verify>
  <done>Agent-search remains cleanly compatible with the shared dual-channel retrieval service, closing the final v1.1 compatibility seam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent-search mode selection -> ordinary retrieval seam | Higher-layer mode control must not bypass the shared retrieval service. |
| enriched retrieval traces -> agent-search reports | Ordinary retrieval trace changes must remain compatible with agent-search output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02 | R | `src/agent/orchestration.rs` / `src/interfaces/cli.rs` | mitigate | Thread mode selection through the shared retrieval seam and test that agent-search never gains a semantic-only bypass path. |
</threat_model>

<verification>
- `cargo test --test agent_search agent_search_reuses_ordinary_retrieval_under_dual_channel_modes -- --nocapture`
- `cargo test --test agent_search dual_channel_mode_selection_preserves_agent_report_contract -- --nocapture`
- `cargo test --test agent_search -- --nocapture`
- `cargo clippy --all-targets -- -D warnings`
</verification>

<success_criteria>
- Agent-search still reuses ordinary retrieval under dual-channel mode selection.
- No semantic-only bypass path is introduced.
- AgentSearchReport remains compatible with enriched retrieval traces.
</success_criteria>

<output>
After completion, create `.planning/phases/10-dual-channel-diagnostics-and-service-compatibility/10-02-SUMMARY.md`
</output>
