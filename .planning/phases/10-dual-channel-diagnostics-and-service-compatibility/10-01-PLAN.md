---
phase: 10-dual-channel-diagnostics-and-service-compatibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/interfaces/cli.rs
  - src/core/status.rs
  - src/core/doctor.rs
  - tests/status_cli.rs
  - tests/retrieval_cli.rs
autonomous: true
requirements:
  - OPS-01
  - OPS-02
must_haves:
  truths:
    - "Operators can tell which retrieval channels are active, disabled, ready, or gated."
    - "Search surfaces can explicitly select lexical-only, embedding-only, or hybrid without destabilizing lexical-only defaults."
    - "Dual-channel diagnostics stay truthful and local-first."
  artifacts:
    - path: "src/interfaces/cli.rs"
      provides: "mode-aware search CLI surface"
    - path: "src/core/status.rs"
      provides: "mode-aware dual-channel diagnostics"
    - path: "tests/status_cli.rs"
      provides: "regression coverage for operator-visible dual-channel readiness semantics"
  key_links:
    - from: "src/interfaces/cli.rs"
      to: "src/search/mod.rs"
      via: "CLI mode selection resolves through the shared SearchService seam"
      pattern: "with_variant"
    - from: "src/core/status.rs"
      to: "src/core/doctor.rs"
      via: "status facts feed mode-aware doctor diagnostics"
      pattern: "DoctorReport::evaluate"
---

<objective>
Make dual-channel retrieval operator-usable by exposing clear mode selection and truthful diagnostics for lexical and embedding/vector readiness.

Purpose: finish the operator surface so developers can intentionally run lexical-only, embedding-only, or hybrid retrieval and understand what is active/gated.
Output: mode-aware search CLI + clearer status/doctor diagnostics + operator regressions.
</objective>

<execution_context>
@/home/tongyuan/.codex/get-shit-done/workflows/execute-plan.md
@/home/tongyuan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-dual-channel-diagnostics-and-service-compatibility/10-CONTEXT.md
@.planning/phases/10-dual-channel-diagnostics-and-service-compatibility/10-RESEARCH.md
@.planning/phases/10-dual-channel-diagnostics-and-service-compatibility/10-PATTERNS.md
@src/interfaces/cli.rs
@src/core/status.rs
@src/core/doctor.rs
@src/search/mod.rs
@tests/status_cli.rs
@tests/retrieval_cli.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add operator-surface regressions for dual-channel mode selection and diagnostics</name>
  <files>tests/status_cli.rs, tests/retrieval_cli.rs</files>
  <read_first>
    - tests/status_cli.rs
    - tests/retrieval_cli.rs
    - src/interfaces/cli.rs
    - src/core/status.rs
    - src/core/doctor.rs
  </read_first>
  <acceptance_criteria>
    - `tests/status_cli.rs` defines `dual_channel_status_and_doctor_report_mode_compatibility_truthfully`
    - `tests/retrieval_cli.rs` defines `search_surface_respects_dual_channel_mode_selection`
    - `cargo test --test status_cli dual_channel_status_and_doctor_report_mode_compatibility_truthfully -- --nocapture` fails before production changes and passes after Task 2
    - the new tests cover lexical-only, embedding-only, and hybrid operator behavior explicitly
  </acceptance_criteria>
  <behavior>
    - Diagnostics must say which channels are active or gated.
    - Search CLI must be able to select modes explicitly.
    - Lexical-only remains stable even with new controls.
  </behavior>
  <action>Extend `tests/status_cli.rs` with `dual_channel_status_and_doctor_report_mode_compatibility_truthfully`, asserting that status/doctor output clearly distinguishes lexical-only, embedding-only, and hybrid readiness/gating states after Phase 9. Extend `tests/retrieval_cli.rs` with `search_surface_respects_dual_channel_mode_selection`, asserting the search CLI can explicitly run lexical-only, embedding-only, and hybrid retrieval behavior through one surface rather than by separate commands or ad hoc fixtures.</action>
  <verify>
    <automated>cargo test --test status_cli dual_channel_status_and_doctor_report_mode_compatibility_truthfully -- --nocapture</automated>
  </verify>
  <done>A failing operator-surface regression harness defines the final dual-channel mode-control and diagnostics contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose dual-channel mode selection through CLI and clarify diagnostics</name>
  <files>src/interfaces/cli.rs, src/core/status.rs, src/core/doctor.rs, tests/status_cli.rs, tests/retrieval_cli.rs</files>
  <read_first>
    - src/interfaces/cli.rs
    - src/core/status.rs
    - src/core/doctor.rs
    - src/search/mod.rs
    - tests/status_cli.rs
    - tests/retrieval_cli.rs
    - .planning/phases/10-dual-channel-diagnostics-and-service-compatibility/10-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - CLI search surface can explicitly choose lexical-only, embedding-only, or hybrid retrieval
    - status/doctor output makes active/gated channel state operator-readable
    - `cargo test --test status_cli -- --nocapture` exits 0
    - `cargo test --test retrieval_cli -- --nocapture` exits 0
  </acceptance_criteria>
  <behavior>
    - One search surface remains in place.
    - Lexical-only defaults remain unchanged when no mode override is supplied.
    - Diagnostics remain detailed, not collapsed into an opaque summary string.
  </behavior>
  <action>In `src/interfaces/cli.rs`, add additive mode selection for the search surface so operators can explicitly run lexical-only, embedding-only, or hybrid retrieval through the same `search` command while preserving lexical-only defaults when no override is supplied. Use the existing config-derived variant / SearchService mode seam rather than inventing a second semantic-only CLI path. In `src/core/status.rs` and `src/core/doctor.rs`, enrich dual-channel diagnostics so operators can tell which channels are active, disabled, ready, or gated without losing the existing capability-state detail. Update the new status/retrieval CLI regressions so they pass with the new operator story.</action>
  <verify>
    <automated>cargo test --test retrieval_cli -- --nocapture</automated>
  </verify>
  <done>Developers can intentionally select dual-channel retrieval modes and understand the system’s channel readiness/gating from the same operator surfaces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator mode selection -> retrieval service dispatch | CLI/library mode selection must map cleanly to the shared retrieval seam. |
| capability-state diagnostics -> operator decisions | Misleading dual-channel diagnostics could cause developers to run the wrong mode or misread readiness. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | R | `src/interfaces/cli.rs` / `src/core/status.rs` / `src/core/doctor.rs` | mitigate | Expose explicit mode selection and truthful channel-state diagnostics so operators cannot misinterpret dual-channel readiness. |
</threat_model>

<verification>
- `cargo test --test status_cli dual_channel_status_and_doctor_report_mode_compatibility_truthfully -- --nocapture`
- `cargo test --test retrieval_cli search_surface_respects_dual_channel_mode_selection -- --nocapture`
- `cargo test --test status_cli -- --nocapture`
- `cargo test --test retrieval_cli -- --nocapture`
- `cargo clippy --all-targets -- -D warnings`
</verification>

<success_criteria>
- CLI/library surfaces can select dual-channel retrieval modes intentionally.
- Status/doctor dual-channel diagnostics are operator-readable and truthful.
- Lexical-only defaults remain stable.
</success_criteria>

<output>
After completion, create `.planning/phases/10-dual-channel-diagnostics-and-service-compatibility/10-01-SUMMARY.md`
</output>
