---
phase: 03-truth-layer-governance
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/memory/mod.rs
  - src/memory/governance.rs
  - tests/truth_governance.rs
autonomous: true
requirements: [TRU-02, TRU-03]
must_haves:
  truths:
    - "T3 promotion attempts remain pending or rejected until explicit evidence review and approval state are recorded."
    - "Approving T3 promotion creates a governed shared-truth outcome without mutating the source T3 record in place."
    - "T3 provenance, confidence, and revocability remain inspectable after promotion reviews are created or rejected."
  artifacts:
    - path: "src/memory/governance.rs"
      provides: "Truth-governance service layer for promotion-review lifecycle and gate enforcement."
    - path: "tests/truth_governance.rs"
      provides: "Integration coverage for blocked and approved T3 -> T2 promotion flows."
  key_links:
    - from: "src/memory/governance.rs"
      to: "src/memory/repository.rs"
      via: "Promotion services orchestrate repository calls without embedding SQL or bypassing audit tables."
      pattern: "create_promotion|approve_promotion|review"
    - from: "src/memory/governance.rs"
      to: "memory_records"
      via: "Approved promotion creates a derived T2 authority row instead of rewriting the source T3 row."
      pattern: "insert_record|derived_from|truth_layer"
---

<objective>
Implement the governed T3 -> T2 promotion lifecycle on top of the Phase 3 foundation.

Purpose: enforce the explicit evidence-review and approval gate required for private T3 hypotheses before anything becomes shared T2 truth, while preserving auditability and revocability.
Output: a truth-governance service with promotion-review commands, gate validation, derived T2 record creation, and integration tests for blocked and approved promotion paths.
</objective>

<execution_context>
@/home/tongyuan/.codex/get-shit-done/workflows/execute-plan.md
@/home/tongyuan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-truth-layer-governance/03-CONTEXT.md
@.planning/phases/03-truth-layer-governance/03-RESEARCH.md
@.planning/phases/03-truth-layer-governance/03-PATTERNS.md
@.planning/phases/03-truth-layer-governance/03-01-SUMMARY.md
@/home/tongyuan/project/agent_memos/src/memory/repository.rs
@/home/tongyuan/project/agent_memos/src/memory/truth.rs

<interfaces>
Expected from `src/memory/truth.rs` after Plan `03-01`:
```rust
pub enum TruthRecord {
    T1 { base: MemoryRecord },
    T2 { base: MemoryRecord, open_candidates: Vec<OntologyCandidateRecord> },
    T3 { base: MemoryRecord, t3_state: T3State, open_reviews: Vec<PromotionReviewRecord> },
}

pub struct T3State {
    pub record_id: String,
    pub confidence_score: f32,
    pub revocation_state: RevocationState,
    pub revoked_at: Option<String>,
    pub revocation_reason: Option<String>,
}

pub struct PromotionReviewRecord {
    pub review_id: String,
    pub source_record_id: String,
    pub result_trigger_state: ReviewState,
    pub evidence_review_state: ReviewState,
    pub consensus_check_state: ReviewState,
    pub metacog_approval_state: ReviewState,
    pub decision_state: PromotionDecisionState,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement promotion-review lifecycle commands over the governance repository seam</name>
  <files>src/memory/mod.rs, src/memory/governance.rs, tests/truth_governance.rs</files>
  <behavior>
    - Callers can create a promotion review for a T3 record, attach evidence, and update the four gate states independently.
    - Promotion commands fail fast when the source record is not T3, when the T3 state is revoked, or when required evidence/review metadata is missing.
    - Governance orchestration lives above repository CRUD and keeps SQL details in repository code, matching the research responsibility split.
  </behavior>
  <action>Create `src/memory/governance.rs` and export it from `src/memory/mod.rs`. Follow the `03-PATTERNS.md` service analog `src/memory/governance.rs` <- `src/ingest/mod.rs`: keep `TruthGovernanceService` synchronous, library-first, request/report oriented, and limited to orchestration plus rule checks while SQL stays behind the repository seam from Plan `03-01`. Implement promotion-review creation, evidence attachment, and four-gate state transitions in line with the locked decisions that T3 is a private working-hypothesis layer with explicit provenance/confidence/revocability and that T3 promotion requires a governed review gate before anything becomes shared T2 truth. Block any path that attempts to promote non-T3 or revoked T3 records. Do not add CLI approval workflows, Rig adapters, or metacognitive action selection in this plan.</action>
  <verify>
    <automated>cargo test --test truth_governance promotion_review_tracks_gate_states_without_promoting -- --nocapture</automated>
  </verify>
  <done>Promotion reviews exist as first-class service operations with explicit gate-state transitions and blocked invalid inputs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce all-gates-passed approval and derive a new T2 authority record</name>
  <files>src/memory/governance.rs, tests/truth_governance.rs</files>
  <behavior>
    - `approve_promotion` fails unless result trigger, evidence review, consensus check, and metacognitive approval are all in the passed state.
    - Approving a promotion creates a new T2 `memory_records` row whose provenance links back to the source T3 record and evidence set.
    - The original T3 row stays T3 and keeps its T3 state so revocation/audit history are not erased by promotion.
  </behavior>
  <action>Extend `TruthGovernanceService` to enforce the full T3 -> T2 gate required by TRU-03 and the locked decision that T3 must never directly overwrite shared truth. Follow the `03-PATTERNS.md` boundary split: keep approval logic in the `src/memory/governance.rs` service layer modeled after `src/ingest/mod.rs`, and keep low-level row persistence in the `src/memory/repository.rs` analog rather than embedding ad hoc SQL in the service. Approval must create a derived T2 authority row instead of updating the source row in place, and the derived row's provenance must reference the source T3 record plus supporting evidence IDs. Add integration tests that prove approval is blocked until all gate states pass and that successful approval leaves the T3 source intact with auditable metadata. Do not add T2 -> T1 candidate logic here; that remains isolated to Plan `03-03`.</action>
  <verify>
    <automated>cargo test --test truth_governance t3_promotion_requires_all_gate_checks -- --nocapture</automated>
  </verify>
  <done>Promotion cannot bypass the explicit gate, approved promotion yields a derived T2 record, and source T3 records remain auditable after approval.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| promotion commands -> governance service | Untrusted approval/evidence inputs cross into state transitions that can affect shared truth. |
| governance service -> authority store | Approved promotion writes a new shared-truth authority row and must not corrupt the source T3 row. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | E | `src/memory/governance.rs` | mitigate | Require all four gate states plus evidence presence before any T2 record can be created. |
| T-03-06 | T | `src/memory/governance.rs` | mitigate | Reject in-place truth-layer mutation and instead create a derived T2 record with provenance links. |
| T-03-07 | R | `tests/truth_governance.rs` | mitigate | Add tests for pending, rejected, and approved paths so audit state transitions remain explicit and reproducible. |
| T-03-08 | I | `src/memory/governance.rs` | mitigate | Preserve T3 confidence/revocation metadata and source provenance after promotion attempts so private-hypothesis audit trails are not lost. |
</threat_model>

<verification>
Run the focused promotion-review test after Task 1 and the gate-enforcement approval test after Task 2, then run the full `truth_governance` suite. This plan is intentionally limited to T3 provenance/revocability and T3 -> T2 promotion enforcement.
</verification>

<success_criteria>
The promotion gate is complete when T3 hypotheses can accumulate evidence and review state, approval is blocked until every required gate passes, and successful promotion creates a derived T2 record without erasing the source T3 audit trail.
</success_criteria>

<output>
After completion, create `.planning/phases/03-truth-layer-governance/03-02-SUMMARY.md`
</output>
