---
phase: 03-truth-layer-governance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/0004_truth_layer_governance.sql
  - src/core/migrations.rs
  - src/memory/mod.rs
  - src/memory/truth.rs
  - src/memory/repository.rs
  - tests/foundation_schema.rs
  - tests/retrieval_cli.rs
  - tests/truth_governance.rs
autonomous: true
requirements: [TRU-01, TRU-02]
must_haves:
  truths:
    - "System can load T1, T2, and T3 as distinct typed truth records instead of treating truth layer as a loose label."
    - "T3 records preserve explicit confidence, revocation state, and provenance-linked governance metadata without changing `memory_records` / `memory_records_fts` authority."
    - "Phase 2 lexical retrieval, filtering, and citations still work after the truth-governance schema lands."
  artifacts:
    - path: "migrations/0004_truth_layer_governance.sql"
      provides: "Additive truth-governance tables and indexes layered on top of `memory_records` and `memory_records_fts`."
    - path: "src/memory/truth.rs"
      provides: "Typed truth-layer models, governance enums, and layer-aware read contracts used by later promotion and candidate plans."
    - path: "src/memory/repository.rs"
      provides: "Authority-row plus governance-table repository queries for typed truth-layer reads."
    - path: "tests/truth_governance.rs"
      provides: "Regression coverage for truth metadata persistence and layer-aware repository semantics."
  key_links:
    - from: "src/core/migrations.rs"
      to: "migrations/0004_truth_layer_governance.sql"
      via: "Truth governance registers as a new additive migration after the lexical sidecar migration."
      pattern: "0004_truth_layer_governance|truth governance"
    - from: "src/memory/repository.rs"
      to: "src/memory/truth.rs"
      via: "Repository reads and writes typed T3 state and truth-layer read models."
      pattern: "TruthRecord|T3State|truth_"
    - from: "tests/retrieval_cli.rs"
      to: "memory_records_fts"
      via: "Compatibility tests prove ordinary retrieval still uses the lexical sidecar over the authority store."
      pattern: "SearchService|citation|truth_layer"
---

<objective>
Add the Phase 3 storage foundation: truth-layer metadata, typed repository contracts, and compatibility-safe query semantics.

Purpose: land the additive schema and typed data-model seam that makes T1/T2/T3 actionable in storage and service APIs without breaking the Phase 2 lexical-first retrieval backbone.
Output: schema version 4, `src/memory/truth.rs`, layer-aware repository queries, and compatibility regressions proving retrieval/citation behavior survives the new governance data.
</objective>

<execution_context>
@/home/tongyuan/.codex/get-shit-done/workflows/execute-plan.md
@/home/tongyuan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-truth-layer-governance/03-CONTEXT.md
@.planning/phases/03-truth-layer-governance/03-RESEARCH.md
@.planning/phases/03-truth-layer-governance/03-PATTERNS.md
@.planning/phases/01-foundation-kernel/01-02-SUMMARY.md
@.planning/phases/02-ingest-and-lightweight-retrieval/02-02-SUMMARY.md
@.planning/phases/02-ingest-and-lightweight-retrieval/02-03-SUMMARY.md
@/home/tongyuan/project/agent_memos/src/core/migrations.rs
@/home/tongyuan/project/agent_memos/src/memory/record.rs
@/home/tongyuan/project/agent_memos/src/memory/repository.rs
@/home/tongyuan/project/agent_memos/src/search/filter.rs
@/home/tongyuan/project/agent_memos/src/search/lexical.rs

<interfaces>
From `src/memory/record.rs`:
```rust
pub struct MemoryRecord {
    pub id: String,
    pub source: SourceRef,
    pub timestamp: RecordTimestamp,
    pub scope: Scope,
    pub record_type: RecordType,
    pub truth_layer: TruthLayer,
    pub provenance: Provenance,
    pub content_text: String,
    pub chunk: Option<ChunkMetadata>,
    pub validity: ValidityWindow,
}
```

From `src/memory/repository.rs`:
```rust
pub struct MemoryRepository<'db> {
    conn: &'db Connection,
}

impl<'db> MemoryRepository<'db> {
    pub fn new(conn: &'db Connection) -> Self;
    pub fn insert_record(&self, record: &MemoryRecord) -> Result<(), RepositoryError>;
    pub fn get_record(&self, id: &str) -> Result<Option<MemoryRecord>, RepositoryError>;
    pub fn list_records(&self) -> Result<Vec<MemoryRecord>, RepositoryError>;
}
```

From `src/search/filter.rs`:
```rust
pub struct SearchFilters {
    pub scope: Option<Scope>,
    pub record_type: Option<RecordType>,
    pub truth_layer: Option<TruthLayer>,
    pub valid_at: Option<String>,
    pub recorded_from: Option<String>,
    pub recorded_to: Option<String>,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add additive truth-governance schema and typed truth metadata models</name>
  <files>migrations/0004_truth_layer_governance.sql, src/core/migrations.rs, src/memory/mod.rs, src/memory/truth.rs, tests/foundation_schema.rs, tests/truth_governance.rs</files>
  <behavior>
    - Schema version 4 adds governance side tables for T3 state, promotion reviews/evidence, and ontology candidates without replacing `memory_records` or `memory_records_fts`.
    - Truth-layer governance models are typed Rust enums/structs, not free-form strings or opaque JSON blobs, so T1/T2/T3 distinctions are explicit in service APIs.
    - T3 state includes explicit confidence, revocation state, and provenance-linked audit metadata per TRU-02.
    - Existing Phase 2 search tables, FTS triggers, and lexical-sidecar objects remain present after migration.
  </behavior>
  <action>Create `migrations/0004_truth_layer_governance.sql` as an additive migration that keeps `memory_records` / `memory_records_fts` authoritative, adds governance side tables only, preserves Phase 2 lexical retrieval/citation compatibility, and prepares later governance seams without implementing Phase 4/5 behavior. Follow the `03-PATTERNS.md` analogs directly: model `migrations/0004_truth_layer_governance.sql` on `migrations/0002_ingest_foundation.sql` for additive `memory_records` evolution plus side tables, register it in `src/core/migrations.rs` with the existing `Migrations::new(... M::up(...) ...)` pattern, and shape `src/memory/truth.rs` after `src/memory/record.rs` with typed enums/structs plus explicit `as_str` / `parse` helpers instead of free-form governance strings. Export the new module from `src/memory/mod.rs`, assert schema version 4 plus side-table/index presence in tests, and do not split content across three authority tables or add Rig, working memory, semantic retrieval, or rumination behavior in this task.</action>
  <verify>
    <automated>cargo test --test foundation_schema foundation_migration_bootstraps_clean_db -- --nocapture</automated>
  </verify>
  <done>Fresh databases migrate to schema version 4 with governance tables present, typed truth metadata exists in Rust code, and schema tests prove the lexical-first storage backbone remains intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend repository queries with layer-aware semantics and retrieval compatibility checks</name>
  <files>src/memory/repository.rs, tests/retrieval_cli.rs, tests/truth_governance.rs, tests/ingest_pipeline.rs</files>
  <behavior>
    - Repository APIs can load T1/T2/T3 as distinct typed read models and query T3 governance metadata without forcing search code to join governance tables.
    - Layer-aware reads expose T3 state and governance summaries through repository/service contracts, satisfying the storage/API half of TRU-01.
    - Ordinary retrieval still returns the same record/citation/filter contract after schema version 4 is applied.
    - The known pre-existing `tests/ingest_pipeline.rs:12` clippy blocker is removed so Phase 3 verification can run cleanly.
  </behavior>
  <action>Extend `src/memory/repository.rs` with the minimum new query surface needed to make truth layers actionable in storage/service APIs while preserving the locked decision that Phase 2 lexical retrieval remains unchanged. Follow the `03-PATTERNS.md` analogs directly: keep SQL and row hydration inside the existing `src/memory/repository.rs` CRUD/`map_*_row` style, use `src/memory/truth.rs` typed models for `TruthRecord` projections and T3-state reads, and leave `src/search/filter.rs` plus `src/search/lexical.rs` on the authority-table path rather than joining governance tables into the default FTS query. Add `tests/truth_governance.rs` cases for typed truth-layer reads and T3-state persistence, add a compatibility regression to `tests/retrieval_cli.rs` proving Phase 2 ordinary retrieval/citations still work on a schema-version-4 database, and clear the known unused import in `tests/ingest_pipeline.rs` so `cargo clippy --all-targets -- -D warnings` is no longer blocked by historical debt. Do not broaden the public search API beyond narrowly scoped compatibility assertions in this plan.</action>
  <verify>
    <automated>cargo test --test truth_governance -- --nocapture && cargo test --test retrieval_cli library_search_returns_citations_and_filter_trace -- --nocapture && cargo clippy --all-targets -- -D warnings</automated>
  </verify>
  <done>Repository callers can query typed truth-layer records and T3 metadata, ordinary retrieval compatibility is proven against the upgraded schema, and clippy no longer fails on the known pre-existing test debt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository inputs -> governance tables | Truth metadata, confidence values, and revocation state cross into durable audit storage. |
| ordinary retrieval -> upgraded schema | Phase 2 search behavior reads through a schema that now contains governance data but must remain ranking-compatible. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T | `migrations/0004_truth_layer_governance.sql` | mitigate | Keep the migration additive and preserve `memory_records` / FTS objects so governance changes cannot silently replace the authority store. |
| T-03-02 | I | `src/memory/truth.rs` | mitigate | Model confidence, revocation, and review states as typed enums/structs instead of free-form strings so governance semantics stay auditable. |
| T-03-03 | R | `src/memory/repository.rs` | mitigate | Store T3 metadata, review rows, and candidate rows in queryable tables so rejected/pending states remain visible instead of collapsing into a final boolean. |
| T-03-04 | D | `tests/retrieval_cli.rs` | mitigate | Add compatibility regressions that prove lexical retrieval still works after migration, preventing a schema-only change from degrading ordinary retrieval unnoticed. |
</threat_model>

<verification>
Run the schema bootstrap test first, then the new truth-governance integration tests, then the focused retrieval compatibility regression, and finally `cargo clippy --all-targets -- -D warnings`. This plan owns the additive data-model seam and verification debt cleanup needed before promotion/candidate services are layered on top.
</verification>

<success_criteria>
Phase 3 foundation is complete when the database can persist typed governance state for T3 and future promotion/candidate flows, repository APIs can distinguish truth layers explicitly, and Phase 2 lexical retrieval still passes unchanged against the upgraded schema.
</success_criteria>

<output>
After completion, create `.planning/phases/03-truth-layer-governance/03-01-SUMMARY.md`
</output>
