---
phase: 03-truth-layer-governance
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - src/memory/governance.rs
  - tests/truth_governance.rs
autonomous: true
requirements: [TRU-01, TRU-04]
must_haves:
  truths:
    - "T2 records can produce T1 ontology candidates without automatically mutating shared T1 truth."
    - "Governance-oriented service APIs can list pending reviews and pending ontology candidates as distinct queues."
    - "T1/T2/T3 remain distinct in service APIs after candidate handling is added."
  artifacts:
    - path: "src/memory/governance.rs"
      provides: "Candidate-first T2 -> T1 service commands and governance read APIs."
    - path: "tests/truth_governance.rs"
      provides: "Integration coverage proving candidate-only behavior and queue-style governance reads."
  key_links:
    - from: "src/memory/governance.rs"
      to: "truth_ontology_candidates"
      via: "T2 -> T1 requests persist as reviewable candidates instead of rewriting T1 records."
      pattern: "create_ontology_candidate|candidate_state"
    - from: "src/memory/governance.rs"
      to: "src/memory/truth.rs"
      via: "Governance read APIs return distinct T1/T2/T3 projections and candidate/review queues."
      pattern: "TruthRecord|pending_reviews|pending_candidates"
---

<objective>
Finish Phase 3 with candidate-first T2 -> T1 handling and governance-oriented read APIs.

Purpose: represent ontology evolution as explicit proposals rather than automatic mutation, and expose the queue-like governance service surface later phases can build on without implementing Phase 4/5 behavior now.
Output: T2 -> T1 candidate commands, pending-review/pending-candidate service APIs, and tests proving candidate-only behavior with no automatic T1 rewrite.
</objective>

<execution_context>
@/home/tongyuan/.codex/get-shit-done/workflows/execute-plan.md
@/home/tongyuan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-truth-layer-governance/03-CONTEXT.md
@.planning/phases/03-truth-layer-governance/03-RESEARCH.md
@.planning/phases/03-truth-layer-governance/03-PATTERNS.md
@.planning/phases/03-truth-layer-governance/03-01-SUMMARY.md
@.planning/phases/03-truth-layer-governance/03-02-SUMMARY.md
@/home/tongyuan/project/agent_memos/src/memory/governance.rs
@/home/tongyuan/project/agent_memos/src/memory/truth.rs

<interfaces>
Expected from `src/memory/governance.rs` after Plan `03-02`:
```rust
pub struct TruthGovernanceService<'db> { /* repository-backed */ }

impl<'db> TruthGovernanceService<'db> {
    pub fn create_promotion_review(&self, source_record_id: &str) -> Result<PromotionReviewRecord, GovernanceError>;
    pub fn attach_promotion_evidence(&self, review_id: &str, evidence_record_id: &str, role: EvidenceRole) -> Result<(), GovernanceError>;
    pub fn record_review_state(&self, review_id: &str, update: PromotionGateUpdate) -> Result<PromotionReviewRecord, GovernanceError>;
    pub fn approve_promotion(&self, review_id: &str, approved_at: &str) -> Result<MemoryRecord, GovernanceError>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add candidate-first T2 -> T1 commands that never mutate T1 automatically</name>
  <files>src/memory/governance.rs, tests/truth_governance.rs</files>
  <behavior>
    - A T2 record can generate an ontology candidate with basis-record references, proposed structure payload, and review-state fields.
    - Candidate creation fails for non-T2 source records and does not create or update any T1 authority row.
    - Candidate records stay explicit, reviewable objects with their own lifecycle state rather than being collapsed into a boolean flag on `memory_records`.
  </behavior>
  <action>Extend `TruthGovernanceService` with T2 -> T1 candidate creation for TRU-04 and the locked decision that T2 -> T1 must be represented as proposal/candidate handling rather than automatic ontology mutation. Follow the `03-PATTERNS.md` analogs directly: keep `src/memory/governance.rs` in the same orchestration-oriented shape as `src/ingest/mod.rs`, and use the typed enums/structs from `src/memory/truth.rs` in the `src/memory/record.rs` style to capture structural-review and candidate-lifecycle state. Persist only explicit ontology-candidate rows and basis references; do not change any T1 `memory_records` row or mutate a T2 source record into T1. Add tests proving candidate creation is blocked for non-T2 inputs and remains candidate-only even after creation.</action>
  <verify>
    <automated>cargo test --test truth_governance t2_to_t1_creates_candidate_without_t1_mutation -- --nocapture</automated>
  </verify>
  <done>T2 -> T1 requests only create explicit candidates, never automatic ontology rewrites, and invalid source layers are rejected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose governance-oriented queue APIs for pending reviews and candidates</name>
  <files>src/memory/governance.rs, tests/truth_governance.rs</files>
  <behavior>
    - Service callers can list pending promotion reviews and pending ontology candidates as distinct governance queues.
    - Service read APIs preserve truth-layer distinctions in their return types so callers can tell whether they are dealing with T1, T2, or T3 data.
    - Ordinary retrieval remains separate from these governance APIs; candidate records do not appear as automatic T1 search results.
  </behavior>
  <action>Add read APIs to `TruthGovernanceService` that list pending promotion reviews, list pending ontology candidates, and load typed layer-aware truth records suitable for later metacognition/rumination callers while preserving the locked decisions that truth layers stay distinct and Phase 2 lexical retrieval behavior remains compatible. Follow the `03-PATTERNS.md` service analog `src/memory/governance.rs` <- `src/ingest/mod.rs` for library-facing request/response structure, and continue using the typed layer models from `src/memory/truth.rs` rather than flattening queues into raw SQL rows or search results. Keep these APIs governance-oriented only; do not wire new CLI subcommands, Rig tools, or search ranking changes in this phase. Extend `tests/truth_governance.rs` to prove queue semantics and candidate-only behavior, including the absence of automatic T1 mutation after candidate creation.</action>
  <verify>
    <automated>cargo test --test truth_governance -- --nocapture</automated>
  </verify>
  <done>Governance callers can inspect pending reviews/candidates through explicit service APIs, and candidate creation remains separate from shared-truth mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| T2 candidate commands -> governance service | Proposed ontology structures and supporting basis records cross into durable governance state. |
| governance queues -> downstream callers | Later phases will consume pending reviews/candidates, so the returned states must stay explicit and non-mutating. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | T | `src/memory/governance.rs` | mitigate | Persist T2 -> T1 requests only as candidate rows and block any path that would update T1 authority records directly. |
| T-03-10 | E | `src/memory/governance.rs` | mitigate | Validate that candidate creation only accepts T2 sources and returns typed queue records instead of raw row mutation hooks. |
| T-03-11 | R | `tests/truth_governance.rs` | mitigate | Add integration coverage that proves pending queues and candidate-only behavior remain explicit and replayable. |
| T-03-12 | I | `src/memory/governance.rs` | mitigate | Keep governance APIs separate from ordinary retrieval so candidate rows do not silently leak into shared-truth search semantics. |
</threat_model>

<verification>
Run the focused candidate-only regression after Task 1, then the full `truth_governance` suite after Task 2. This plan intentionally stops at candidate creation and governance read APIs; it does not implement ontology approval, metacognitive reviewers, or any Rig/working-memory features.
</verification>

<success_criteria>
Phase 3 is complete when T2 -> T1 evolution is represented only through explicit candidates, governance callers can inspect pending review/candidate queues through typed service APIs, and T1/T2/T3 remain distinct without changing the Phase 2 lexical-first retrieval contract.
</success_criteria>

<output>
After completion, create `.planning/phases/03-truth-layer-governance/03-03-SUMMARY.md`
</output>
