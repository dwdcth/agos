---
phase: 06-runtime-gate-enforcement
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/core/doctor.rs
  - src/core/status.rs
  - src/interfaces/cli.rs
  - tests/status_cli.rs
  - tests/runtime_gate_cli.rs
autonomous: true
requirements:
  - FND-01
  - FND-03
  - AGT-01
must_haves:
  truths:
    - "Operational commands are blocked not only for reserved/invalid semantic configs, but also for lexical runtime states that are demonstrably not executable."
    - "`status`, `doctor`, and `inspect schema` remain trustworthy diagnostic surfaces after runtime gate propagation."
    - "The gate behavior is regression-tested across multiple local-state conditions so the bypass cannot silently return in a later phase."
  artifacts:
    - path: "tests/status_cli.rs"
      provides: "Diagnostic-surface regression coverage that keeps `status`/`doctor`/schema inspection semantics stable"
    - path: "tests/runtime_gate_cli.rs"
      provides: "Negative-path runtime readiness coverage for missing init, bad DB files, and missing lexical sidecar states"
    - path: "src/core/doctor.rs"
      provides: "Finalized operator-facing failure/warning semantics after the regression matrix is locked"
  key_links:
    - from: "tests/status_cli.rs"
      to: "src/core/status.rs"
      via: "diagnostic exit semantics and exact rendered text"
      pattern: "status / doctor / inspect schema"
    - from: "tests/runtime_gate_cli.rs"
      to: "src/interfaces/cli.rs"
      via: "negative-path lexical readiness matrix for operational commands"
      pattern: "run_cli"
    - from: "src/core/doctor.rs"
      to: "src/core/status.rs"
      via: "status-derived readiness facts remain the only source of operational failure synthesis"
      pattern: "StatusReport"
---

<objective>
Lock the regression matrix and operator semantics around the new runtime gate so future phases cannot silently reopen the bypass or degrade the informational diagnostics contract.

Purpose: turn the Phase 6 enforcement fix into a stable, audited behavior set across reserved semantic modes, missing-init lexical states, broken local DB files, and informational command surfaces.
Output: a complete CLI regression matrix plus any small diagnostic cleanups required to keep gate output and informational commands consistent.
</objective>

<execution_context>
@/home/tongyuan/.codex/get-shit-done/workflows/execute-plan.md
@/home/tongyuan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-runtime-gate-enforcement/06-CONTEXT.md
@.planning/phases/06-runtime-gate-enforcement/06-RESEARCH.md
@.planning/phases/06-runtime-gate-enforcement/06-PATTERNS.md
@.planning/phases/06-runtime-gate-enforcement/06-VALIDATION.md
@.planning/phases/01-foundation-kernel/01-04-SUMMARY.md
@src/core/status.rs
@src/core/doctor.rs
@src/interfaces/cli.rs
@tests/status_cli.rs
@tests/runtime_gate_cli.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add lexical not-ready and broken-local-state regression coverage for operational commands</name>
  <files>tests/runtime_gate_cli.rs, src/core/doctor.rs, src/core/status.rs, src/interfaces/cli.rs</files>
  <read_first>
    - tests/runtime_gate_cli.rs
    - tests/status_cli.rs
    - src/core/status.rs
    - src/core/doctor.rs
    - src/interfaces/cli.rs
  </read_first>
  <acceptance_criteria>
    - `tests/runtime_gate_cli.rs` contains `operational_commands_block_when_runtime_readiness_is_not_satisfied`
    - that test asserts non-zero exit for lexical configurations with missing init, unreadable/non-SQLite DB files, and missing lexical sidecar indexes
    - the blocked output contains doctor-style `ready: false` plus at least one concrete failure reason derived from status facts
    - `cargo test --test runtime_gate_cli operational_commands_block_when_runtime_readiness_is_not_satisfied -- --nocapture` exits 0
  </acceptance_criteria>
  <behavior>
    - Operational blocking must extend beyond reserved semantic modes to true lexical not-ready states.
    - Broken local DB files should block operational commands with explanatory diagnostics instead of surfacing lower-level rusqlite errors.
    - Missing lexical sidecar state should block lexical operational commands consistently.
  </behavior>
  <action>Extend `tests/runtime_gate_cli.rs` with one test named `operational_commands_block_when_runtime_readiness_is_not_satisfied`. Cover three concrete cases: `(1)` config points at a missing DB path with `retrieval.mode = "lexical_only"` and `embedding.backend = "disabled"`, `(2)` config points at an existing file containing non-SQLite bytes, `(3)` config points at a SQLite file where the foundation schema exists but lexical sidecar tables/triggers are intentionally absent. For each case, loop over `ingest`, `search`, and `agent-search`, assert non-zero exit, and assert that stdout contains `ready: false` plus the exact blocking reason produced from `StatusReport`/`DoctorReport`. If the new regression exposes mismatched or duplicated wording in `src/core/doctor.rs`, `src/core/status.rs`, or `src/interfaces/cli.rs`, adjust those files minimally so operational failures stay structured and deterministic without changing the diagnostic-only command contract.</action>
  <verify>
    <automated>cargo test --test runtime_gate_cli operational_commands_block_when_runtime_readiness_is_not_satisfied -- --nocapture</automated>
  </verify>
  <done>The runtime gate now covers real lexical not-ready states in addition to reserved/invalid semantic configurations, and failures remain explainable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve informational command semantics while freezing the new gate contract</name>
  <files>tests/status_cli.rs, tests/runtime_gate_cli.rs, src/core/doctor.rs, src/interfaces/cli.rs</files>
  <read_first>
    - tests/status_cli.rs
    - tests/runtime_gate_cli.rs
    - src/core/doctor.rs
    - src/interfaces/cli.rs
    - .planning/phases/01-foundation-kernel/01-04-SUMMARY.md
  </read_first>
  <acceptance_criteria>
    - `tests/status_cli.rs` contains `diagnostic_commands_remain_informational_while_operational_gate_uses_same_contract`
    - the test proves `status` still exits successfully for reserved modes and broken local DB files
    - the test proves `inspect schema` remains diagnostic-only while operational commands still fail on the same config/state
    - `cargo test --test status_cli diagnostic_commands_remain_informational_while_operational_gate_uses_same_contract -- --nocapture` exits 0
    - `cargo test --test runtime_gate_cli -- --nocapture` and `cargo test --test status_cli -- --nocapture` both exit 0
  </acceptance_criteria>
  <behavior>
    - Informational commands remain truthful operator tools rather than becoming hard-stop execution paths.
    - Operational gate output stays aligned with the same doctor-style structure used by explicit diagnostics.
    - The regression matrix should be robust enough that later phases cannot accidentally move the gate back below DB/service execution.
  </behavior>
  <action>Add a new `tests/status_cli.rs` test named `diagnostic_commands_remain_informational_while_operational_gate_uses_same_contract`. Use temp-dir fixtures to prove that, under reserved semantic modes and broken local DB files, `status` still exits 0 and reports explicit non-ready/readiness-note output, while `doctor` and the operational commands expose the same structured failure reasons without raw lower-level exceptions. Keep `inspect schema` exit semantics diagnostic-only. If the test exposes formatting drift between explicit `doctor` and operational-gate output, make the smallest possible adjustment in `src/core/doctor.rs` or the shared CLI gate helper so both paths print the same `DoctorReport::render_text()` contract.</action>
  <verify>
    <automated>cargo test --test status_cli diagnostic_commands_remain_informational_while_operational_gate_uses_same_contract -- --nocapture</automated>
  </verify>
  <done>The new runtime gate is locked behind a regression matrix that preserves the original diagnostic contract and makes future bypass regressions obvious.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| status facts -> operational failures | Non-ready local state must be translated into user-visible blocking reasons without losing clarity or inventing a second rule set. |
| diagnostic commands -> operational commands | The same runtime facts must yield different exit semantics depending on whether the command is informational or operational. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | T | `tests/runtime_gate_cli.rs` | mitigate | Freeze the negative-path matrix for missing init, bad DB files, and missing lexical sidecars so later refactors cannot silently bypass the gate. |
| T-06-02 | D | `src/interfaces/cli.rs` | mitigate | Block operational commands before DB/service execution, preventing broken local state from degrading into confusing downstream runtime errors. |
| T-06-03 | R | `tests/status_cli.rs` / `src/core/doctor.rs` | mitigate | Keep informational and operational output on the same structured `DoctorReport` / `StatusReport` contract so operators can reproduce why a command was allowed or blocked. |
</threat_model>

<verification>
- `cargo test --test runtime_gate_cli operational_commands_block_when_runtime_readiness_is_not_satisfied -- --nocapture`
- `cargo test --test status_cli diagnostic_commands_remain_informational_while_operational_gate_uses_same_contract -- --nocapture`
- `cargo test --test runtime_gate_cli -- --nocapture`
- `cargo test --test status_cli -- --nocapture`
- `cargo test --tests && cargo clippy --all-targets -- -D warnings`
</verification>

<success_criteria>
- Runtime gate behavior is regression-covered for reserved/invalid semantic modes and real lexical not-ready states.
- Diagnostic commands still behave as informational operator tools after Phase 6 lands.
- Operational failures remain structured, explainable, and consistent across command entrypoints.
</success_criteria>

<output>
After completion, create `.planning/phases/06-runtime-gate-enforcement/06-02-SUMMARY.md`
</output>
