---
phase: 07-follow-up-evidence-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cognition/assembly.rs
  - src/cognition/working_memory.rs
  - tests/working_memory_assembly.rs
autonomous: true
requirements:
  - COG-01
  - AGT-02
must_haves:
  truths:
    - "Follow-up retrieval evidence is integrated into the assembled runtime working-memory object rather than remaining only in report trace data."
    - "`present.world_fragments` and branch supporting evidence can both see merged follow-up evidence with citations and truth context intact."
    - "Primary-query-only assembly behavior remains backward compatible when no follow-up evidence is supplied."
  artifacts:
    - path: "src/cognition/assembly.rs"
      provides: "Additive assembler input and merge logic for primary + follow-up retrieval evidence"
    - path: "src/cognition/working_memory.rs"
      provides: "Runtime evidence model that can carry integrated follow-up provenance without splitting the cognitive container"
    - path: "tests/working_memory_assembly.rs"
      provides: "Regression coverage for merged world fragments and branch evidence"
  key_links:
    - from: "src/cognition/assembly.rs"
      to: "src/cognition/working_memory.rs"
      via: "merged `EvidenceFragment` materialization into `PresentFrame.world_fragments`"
      pattern: "EvidenceFragment"
    - from: "src/cognition/assembly.rs"
      to: "tests/working_memory_assembly.rs"
      via: "real assembler integration tests over persisted records and merged evidence"
      pattern: "WorkingMemoryAssembler"
---

<objective>
Repair the assembler seam so follow-up retrieval evidence becomes part of the real runtime cognition state instead of staying trapped in agent-search trace output.

Purpose: close the audit gap at the assembly boundary first, so later scoring/gate/report work can consume one coherent evidence set.
Output: additive working-memory request/assembler support for merged evidence plus assembly-level regression coverage.
</objective>

<execution_context>
@/home/tongyuan/.codex/get-shit-done/workflows/execute-plan.md
@/home/tongyuan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-follow-up-evidence-integration/07-CONTEXT.md
@.planning/phases/07-follow-up-evidence-integration/07-RESEARCH.md
@.planning/phases/07-follow-up-evidence-integration/07-PATTERNS.md
@.planning/phases/04-working-memory-and-agent-search/04-CONTEXT.md
@.planning/phases/04-working-memory-and-agent-search/04-01-SUMMARY.md
@.planning/phases/04-working-memory-and-agent-search/04-03-SUMMARY.md
@src/cognition/assembly.rs
@src/cognition/working_memory.rs
@src/agent/orchestration.rs
@tests/working_memory_assembly.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add assembly regressions for integrated follow-up evidence</name>
  <files>tests/working_memory_assembly.rs</files>
  <read_first>
    - tests/working_memory_assembly.rs
    - src/cognition/assembly.rs
    - src/cognition/working_memory.rs
    - src/agent/orchestration.rs
  </read_first>
  <acceptance_criteria>
    - `tests/working_memory_assembly.rs` defines `assembler_integrates_follow_up_evidence_into_world_fragments`
    - `tests/working_memory_assembly.rs` defines `assembler_uses_integrated_follow_up_fragments_for_branch_support`
    - `cargo test --test working_memory_assembly assembler_integrates_follow_up_evidence_into_world_fragments -- --nocapture` fails before production changes and passes after Task 2
    - the new tests assert on integrated fragment record IDs, citations, and branch supporting evidence rather than on report-only data
  </acceptance_criteria>
  <behavior>
    - The assembler must support an integrated evidence set wider than the primary query alone.
    - Integrated follow-up evidence must preserve citation/truth-context shape.
    - Branch materialization must be able to resolve supporting record IDs from merged evidence, not only primary-query recall.
  </behavior>
  <action>Extend `tests/working_memory_assembly.rs` with two new red-green regressions. Add `assembler_integrates_follow_up_evidence_into_world_fragments`, which persists at least one primary-query record and one follow-up-only record, supplies both through the new additive assembler-input path, and asserts that `working_memory.present.world_fragments` contains both record IDs with intact citations and truth context. Add `assembler_uses_integrated_follow_up_fragments_for_branch_support`, which seeds an action branch that references the follow-up-only record ID and asserts the assembled branch `supporting_evidence` includes that integrated fragment. Keep the tests on the real `WorkingMemoryAssembler`, not a mocked report layer.</action>
  <verify>
    <automated>cargo test --test working_memory_assembly assembler_integrates_follow_up_evidence_into_world_fragments -- --nocapture</automated>
  </verify>
  <done>A failing assembly-level regression harness exists that proves follow-up evidence still does not enter working memory until Task 2 changes production code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend working-memory request and assembler to merge follow-up evidence</name>
  <files>src/cognition/assembly.rs, src/cognition/working_memory.rs, tests/working_memory_assembly.rs</files>
  <read_first>
    - src/cognition/assembly.rs
    - src/cognition/working_memory.rs
    - tests/working_memory_assembly.rs
    - .planning/phases/07-follow-up-evidence-integration/07-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - `src/cognition/assembly.rs` contains an additive request field or builder method for externally supplied retrieved evidence
    - `WorkingMemoryAssembler::assemble` merges supplied evidence with primary-query results before truth projection / fragment materialization
    - `present.world_fragments` and branch `supporting_evidence` are both sourced from the same merged fragment list
    - `cargo test --test working_memory_assembly -- --nocapture` exits 0
  </acceptance_criteria>
  <behavior>
    - No supplied follow-up evidence: preserve current primary-query-only behavior.
    - Supplied follow-up evidence: merge/dedupe it before fragment materialization so cognition uses one evidence set.
    - Citation and truth-context fidelity must survive the merge.
  </behavior>
  <action>In `src/cognition/assembly.rs`, extend `WorkingMemoryRequest` with an additive typed field for externally supplied retrieved evidence and add builder helpers such as `with_integrated_results(...)` or equivalent. Keep `query`, `filters`, action seeds, and local adaptation behavior intact. Refactor `WorkingMemoryAssembler::assemble` so it still performs the existing search when no external evidence is supplied, but when integrated results are present it merges the primary-query results with those supplied results, dedupes by record identity, then runs one truth-projection/materialization pass to build `EvidenceFragment`s. Ensure `present.world_fragments` and `materialize_branch(...)` both operate on that merged fragment list. If `src/cognition/working_memory.rs` needs additive provenance metadata to represent merged evidence clearly, add it there without changing the top-level `WorkingMemory { present, branches }` contract.</action>
  <verify>
    <automated>cargo test --test working_memory_assembly -- --nocapture</automated>
  </verify>
  <done>Working-memory assembly now treats follow-up evidence as real runtime cognition input, not as a report-only afterthought.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent-search retrieval loop -> working-memory assembler | Multi-step retrieval evidence crosses from orchestration into the runtime cognition object here. |
| merged evidence set -> branch materialization | Supporting record IDs must resolve against the integrated fragment set without silently dropping follow-up-only evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | T | `src/cognition/assembly.rs` | mitigate | Merge follow-up evidence through one typed assembler path so no second hidden evidence channel can drift from working-memory state. |
| T-07-02 | R | `tests/working_memory_assembly.rs` | mitigate | Lock integration behavior with assembly-level regressions that assert merged fragments and branch evidence contain follow-up-only records. |
</threat_model>

<verification>
- `cargo test --test working_memory_assembly assembler_integrates_follow_up_evidence_into_world_fragments -- --nocapture`
- `cargo test --test working_memory_assembly assembler_uses_integrated_follow_up_fragments_for_branch_support -- --nocapture`
- `cargo test --test working_memory_assembly -- --nocapture`
- `cargo clippy --all-targets -- -D warnings`
</verification>

<success_criteria>
- Follow-up evidence is merged into `WorkingMemory.present.world_fragments`.
- Branch supporting evidence can resolve integrated follow-up fragments.
- Ordinary assembly callers still work when no follow-up evidence is supplied.
</success_criteria>

<output>
After completion, create `.planning/phases/07-follow-up-evidence-integration/07-01-SUMMARY.md`
</output>
