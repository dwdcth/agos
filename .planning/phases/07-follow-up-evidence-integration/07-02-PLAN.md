---
phase: 07-follow-up-evidence-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/agent/orchestration.rs
  - tests/agent_search.rs
autonomous: true
requirements:
  - AGT-02
  - AGT-03
  - COG-01
must_haves:
  truths:
    - "Agent-search decisions are now made over the same merged evidence set that the report exposes."
    - "`retrieval_steps`, top-level citations, `working_memory`, and `decision` no longer describe different evidence universes."
    - "Follow-up evidence remains query-traceable even after being integrated into cognition."
  artifacts:
    - path: "src/agent/orchestration.rs"
      provides: "Orchestrator wiring that feeds merged follow-up evidence into assembly while preserving per-step trace reports"
    - path: "tests/agent_search.rs"
      provides: "Integration regressions proving follow-up evidence affects working memory, report alignment, and decision surface"
  key_links:
    - from: "src/agent/orchestration.rs"
      to: "src/cognition/assembly.rs"
      via: "integrated evidence passed into `WorkingMemoryRequest` before assembly"
      pattern: "WorkingMemoryRequest"
    - from: "tests/agent_search.rs"
      to: "src/agent/orchestration.rs"
      via: "scripted retrieval + report assertions over merged follow-up evidence"
      pattern: "AgentSearchOrchestrator"
---

<objective>
Finish the cognition repair by wiring merged follow-up evidence through orchestration and proving that the final report, branch scoring, and gate output all reflect the same evidence set.

Purpose: close the remaining audit seam where follow-up evidence is visible in `AgentSearchReport` but not actually used for decision selection.
Output: orchestrator/report alignment plus integration tests over agent-search behavior.
</objective>

<execution_context>
@/home/tongyuan/.codex/get-shit-done/workflows/execute-plan.md
@/home/tongyuan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-follow-up-evidence-integration/07-CONTEXT.md
@.planning/phases/07-follow-up-evidence-integration/07-RESEARCH.md
@.planning/phases/07-follow-up-evidence-integration/07-PATTERNS.md
@.planning/phases/07-follow-up-evidence-integration/07-01-PLAN.md
@src/agent/orchestration.rs
@src/cognition/assembly.rs
@src/cognition/metacog.rs
@src/cognition/value.rs
@tests/agent_search.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add agent-search regressions for merged follow-up evidence and decision consistency</name>
  <files>tests/agent_search.rs</files>
  <read_first>
    - tests/agent_search.rs
    - src/agent/orchestration.rs
    - src/cognition/assembly.rs
    - src/cognition/metacog.rs
    - src/cognition/value.rs
  </read_first>
  <acceptance_criteria>
    - `tests/agent_search.rs` defines `orchestrator_integrates_follow_up_evidence_into_working_memory_and_report`
    - `tests/agent_search.rs` defines `integrated_follow_up_evidence_influences_decision_surface`
    - `cargo test --test agent_search orchestrator_integrates_follow_up_evidence_into_working_memory_and_report -- --nocapture` fails before production wiring changes and passes after Task 2
    - the new tests assert on `working_memory`, `citations`, and `decision` together instead of only `retrieval_steps`
  </acceptance_criteria>
  <behavior>
    - Follow-up-only citations must appear in integrated working memory after orchestration.
    - The selected/regulated branch path must be explainable from the merged evidence set.
    - Query-step traceability must remain visible in `retrieval_steps`.
  </behavior>
  <action>Extend `tests/agent_search.rs` with two new orchestrator-level regressions. Add `orchestrator_integrates_follow_up_evidence_into_working_memory_and_report`, using scripted retrieval responses where a follow-up query returns a record absent from the primary query, and assert that the final `AgentSearchReport.working_memory.present.world_fragments` contains that record while `retrieval_steps` and top-level `citations` still preserve query-step traceability. Add `integrated_follow_up_evidence_influences_decision_surface`, using scripted assembly/scoring/gate fixtures or the real production ports as appropriate, and assert that branch support / gate diagnostics now reflect the merged evidence set rather than only the primary query evidence.</action>
  <verify>
    <automated>cargo test --test agent_search orchestrator_integrates_follow_up_evidence_into_working_memory_and_report -- --nocapture</automated>
  </verify>
  <done>A failing orchestration regression harness exists that exposes the remaining mismatch between reported follow-up evidence and actual decision input.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire follow-up evidence through orchestration and keep report semantics aligned</name>
  <files>src/agent/orchestration.rs, tests/agent_search.rs</files>
  <read_first>
    - src/agent/orchestration.rs
    - src/cognition/assembly.rs
    - tests/agent_search.rs
    - .planning/phases/07-follow-up-evidence-integration/07-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - `src/agent/orchestration.rs` passes merged follow-up evidence into the working-memory assembly request before calling `assemble(...)`
    - `AgentSearchReport.working_memory.present.world_fragments` can contain follow-up-only evidence that also appears in `retrieval_steps` / top-level citations
    - `cargo test --test agent_search -- --nocapture` exits 0
    - `cargo test --tests && cargo clippy --all-targets -- -D warnings` exits 0
  </acceptance_criteria>
  <behavior>
    - The bounded retrieval loop and `retrieval_steps` trace shape remain intact.
    - Decision selection and gate evaluation now operate over the same merged evidence set exposed in the final report.
    - No new Rig-facing cognition path is introduced.
  </behavior>
  <action>In `src/agent/orchestration.rs`, keep the existing bounded query loop and `RetrievalStepReport` generation, but also accumulate the underlying retrieved result data needed by the new additive assembly seam from Plan 07-01. Before calling `assemble(...)`, create an updated `WorkingMemoryRequest` that includes the merged follow-up evidence. Ensure the final `AgentSearchReport` still exposes `retrieval_steps`, deduplicated citations, `working_memory`, and `decision`, but now those fields all describe the same integrated evidence set. Update the new tests in `tests/agent_search.rs` so they assert that follow-up-only evidence is visible both in the report trace and in the actual runtime cognition object used for branch comparison and gate evaluation.</action>
  <verify>
    <automated>cargo test --test agent_search -- --nocapture</automated>
  </verify>
  <done>Agent-search no longer reports evidence that the cognition core ignored; report trace and decision surface are aligned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orchestrator retrieval reports -> working-memory request | Follow-up query outputs cross from trace collection into decision-making input here. |
| merged working memory -> score/gate/report | Scoring, metacognition, and structured reporting all depend on the same integrated evidence set after this handoff. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02 | T | `src/agent/orchestration.rs` | mitigate | Feed follow-up retrieval evidence into the assembler through one typed request path so report trace and decision input cannot diverge silently. |
| T-07-03 | R | `tests/agent_search.rs` | mitigate | Lock report/decision consistency with orchestration regressions that assert merged evidence influences `working_memory`, citations, and gate outcomes together. |
</threat_model>

<verification>
- `cargo test --test agent_search orchestrator_integrates_follow_up_evidence_into_working_memory_and_report -- --nocapture`
- `cargo test --test agent_search integrated_follow_up_evidence_influences_decision_surface -- --nocapture`
- `cargo test --test agent_search -- --nocapture`
- `cargo test --tests && cargo clippy --all-targets -- -D warnings`
</verification>

<success_criteria>
- Agent-search decisions are made over integrated primary + follow-up evidence.
- `AgentSearchReport` trace fields and runtime cognition fields stay aligned.
- Query-step provenance remains visible after evidence integration.
</success_criteria>

<output>
After completion, create `.planning/phases/07-follow-up-evidence-integration/07-02-SUMMARY.md`
</output>
