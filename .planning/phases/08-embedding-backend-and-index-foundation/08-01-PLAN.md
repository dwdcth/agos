---
phase: 08-embedding-backend-and-index-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/config.rs
  - src/core/app.rs
  - src/core/status.rs
  - src/core/doctor.rs
  - tests/status_cli.rs
autonomous: true
requirements:
  - EMB-01
must_haves:
  truths:
    - "Developers can configure a real optional embedding backend/model without altering lexical-only defaults."
    - "Status/doctor diagnostics expose embedding backend readiness truthfully and keep lexical-first semantics explicit."
    - "Phase 8 foundation work does not accidentally make embedding-first retrieval the default or implicit runtime path."
  artifacts:
    - path: "src/core/config.rs"
      provides: "Typed embedding backend/model config contract"
    - path: "src/core/status.rs"
      provides: "Embedding-capability state reporting alongside lexical readiness"
    - path: "tests/status_cli.rs"
      provides: "Regression coverage for embedding backend readiness semantics"
  key_links:
    - from: "src/core/config.rs"
      to: "src/core/app.rs"
      via: "typed config feeds runtime readiness derivation"
      pattern: "RuntimeReadiness::from_config"
    - from: "src/core/status.rs"
      to: "src/core/doctor.rs"
      via: "status facts feed operator-facing warning/failure policy"
      pattern: "DoctorReport::evaluate"
---

<objective>
Establish real embedding backend configuration and readiness semantics while preserving the project’s lexical-first default behavior.

Purpose: make semantic capability explicit and inspectable before any embedding persistence or vector-sidecar work lands.
Output: typed embedding backend config, truthful readiness/status/doctor behavior, and config/operator regressions.
</objective>

<execution_context>
@/home/tongyuan/.codex/get-shit-done/workflows/execute-plan.md
@/home/tongyuan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-embedding-backend-and-index-foundation/08-CONTEXT.md
@.planning/phases/08-embedding-backend-and-index-foundation/08-RESEARCH.md
@.planning/phases/08-embedding-backend-and-index-foundation/08-PATTERNS.md
@src/core/config.rs
@src/core/app.rs
@src/core/status.rs
@src/core/doctor.rs
@tests/status_cli.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add status/doctor regressions for real embedding backend states</name>
  <files>tests/status_cli.rs</files>
  <read_first>
    - tests/status_cli.rs
    - src/core/config.rs
    - src/core/app.rs
    - src/core/status.rs
    - src/core/doctor.rs
  </read_first>
  <acceptance_criteria>
    - `tests/status_cli.rs` defines `embedding_foundation_status_reports_backend_readiness_truthfully`
    - `tests/status_cli.rs` defines `embedding_foundation_doctor_preserves_lexical_first_contract`
    - `cargo test --test status_cli embedding_foundation_status_reports_backend_readiness_truthfully -- --nocapture` fails before production changes and passes after Task 2
    - the new tests cover disabled, configured-but-not-ready, and ready-style embedding backend states without changing lexical-only defaults
  </acceptance_criteria>
  <behavior>
    - Embedding backend config must stay explicit and typed.
    - Diagnostics must distinguish disabled vs configured-but-not-ready vs ready-ish backend states.
    - Lexical-first semantics must remain obvious in status/doctor output.
  </behavior>
  <action>Extend `tests/status_cli.rs` with red-green coverage for real embedding backend semantics. Add `embedding_foundation_status_reports_backend_readiness_truthfully`, covering at least one new implementation-ready embedding backend variant plus the existing disabled path, and assert that `status` reports explicit embedding dependency/readiness states and notes without changing lexical-only defaults. Add `embedding_foundation_doctor_preserves_lexical_first_contract`, asserting that `doctor` still treats lexical-only as the baseline while reporting truthful failures/warnings for embedding-only and hybrid modes under the new backend states.</action>
  <verify>
    <automated>cargo test --test status_cli embedding_foundation_status_reports_backend_readiness_truthfully -- --nocapture</automated>
  </verify>
  <done>A failing operator-surface regression harness exists that defines the real embedding backend/readiness contract before production code changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend config/runtime readiness/status for optional embedding backend support</name>
  <files>src/core/config.rs, src/core/app.rs, src/core/status.rs, src/core/doctor.rs, tests/status_cli.rs</files>
  <read_first>
    - src/core/config.rs
    - src/core/app.rs
    - src/core/status.rs
    - src/core/doctor.rs
    - tests/status_cli.rs
    - .planning/phases/08-embedding-backend-and-index-foundation/08-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - `src/core/config.rs` contains at least one real embedding backend variant in `EmbeddingBackend`
    - `src/core/app.rs` and `src/core/status.rs` expose truthful embedding readiness semantics without collapsing them into a boolean flag
    - `src/core/doctor.rs` preserves lexical-first operator semantics while handling the extended embedding states
    - `cargo test --test status_cli -- --nocapture` exits 0
  </acceptance_criteria>
  <behavior>
    - Lexical-only remains the default and unaffected when embedding is disabled.
    - Embedding config is optional and inspectable.
    - Phase 8 foundation work must not accidentally make semantic retrieval runnable as the primary path.
  </behavior>
  <action>In `src/core/config.rs`, extend `EmbeddingBackend` beyond `Disabled` / `Reserved` with at least one concrete implementation-ready backend variant and keep `EmbeddingConfig { backend, model, endpoint }` typed and optional. In `src/core/app.rs`, update `RuntimeReadiness::from_config(...)` so lexical-only defaults stay intact while embedding-specific notes describe the new backend states truthfully. In `src/core/status.rs`, expand embedding dependency/readiness reporting so operators can distinguish disabled vs configured-but-not-ready vs ready embedding substrate states without weakening lexical-first status behavior. In `src/core/doctor.rs`, preserve the existing lexical-first contract and explicit impossible/reserved semantics while handling the extended backend variants consistently. Keep all changes diagnostic/foundation-only — do not yet add embedding retrieval execution.</action>
  <verify>
    <automated>cargo test --test status_cli -- --nocapture</automated>
  </verify>
  <done>The codebase has a real, typed, optional embedding backend config and truthful operator-visible readiness semantics that leave lexical-first behavior unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config file -> runtime readiness | Embedding backend/model choices now influence readiness and diagnostics. |
| status facts -> operator decisions | Misreporting embedding state could cause users to assume semantic capability exists when it does not. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | R | `src/core/status.rs` / `src/core/doctor.rs` | mitigate | Expose typed embedding backend/readiness states and test them so diagnostics cannot silently lie about semantic capability. |
</threat_model>

<verification>
- `cargo test --test status_cli embedding_foundation_status_reports_backend_readiness_truthfully -- --nocapture`
- `cargo test --test status_cli embedding_foundation_doctor_preserves_lexical_first_contract -- --nocapture`
- `cargo test --test status_cli -- --nocapture`
- `cargo clippy --all-targets -- -D warnings`
</verification>

<success_criteria>
- Embedding backend config is typed and optional.
- Status/doctor truthfully report embedding readiness while lexical-only remains the baseline.
- No semantic-primary behavior is introduced yet.
</success_criteria>

<output>
After completion, create `.planning/phases/08-embedding-backend-and-index-foundation/08-01-SUMMARY.md`
</output>
