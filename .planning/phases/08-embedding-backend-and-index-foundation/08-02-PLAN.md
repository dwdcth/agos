---
phase: 08-embedding-backend-and-index-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - migrations/0006_embedding_foundation.sql
  - src/core/migrations.rs
  - src/ingest/mod.rs
  - src/memory/repository.rs
  - tests/ingest_pipeline.rs
  - tests/foundation_schema.rs
autonomous: true
requirements:
  - EMB-02
must_haves:
  truths:
    - "Chunk-aligned embeddings are persisted additively without replacing `memory_records` as the authority store."
    - "Lexical-only ingest still works when embeddings are disabled or unavailable."
    - "Embedding storage aligns with the existing retrieval/citation grain."
  artifacts:
    - path: "migrations/0006_embedding_foundation.sql"
      provides: "Additive embedding persistence tables keyed to authority records"
    - path: "src/ingest/mod.rs"
      provides: "Embedding persistence hook attached to ingest completion"
    - path: "tests/ingest_pipeline.rs"
      provides: "Regression coverage for chunk-aligned embedding persistence"
  key_links:
    - from: "src/ingest/mod.rs"
      to: "src/memory/repository.rs"
      via: "embedding side-table persistence following authority-row creation"
      pattern: "MemoryRepository"
    - from: "migrations/0006_embedding_foundation.sql"
      to: "tests/foundation_schema.rs"
      via: "additive schema bootstrap and foreign-key alignment"
      pattern: "foundation schema"
---

<objective>
Persist chunk-aligned embeddings additively at the ingest/storage layer so the semantic substrate becomes real without changing authority ownership or lexical-only ingest behavior.

Purpose: create the embedding data foundation that Phase 9 will later consume for dual-channel retrieval.
Output: additive embedding schema plus ingest-time embedding persistence and regression coverage.
</objective>

<execution_context>
@/home/tongyuan/.codex/get-shit-done/workflows/execute-plan.md
@/home/tongyuan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-embedding-backend-and-index-foundation/08-CONTEXT.md
@.planning/phases/08-embedding-backend-and-index-foundation/08-RESEARCH.md
@.planning/phases/08-embedding-backend-and-index-foundation/08-PATTERNS.md
@src/ingest/mod.rs
@src/ingest/chunk.rs
@src/memory/repository.rs
@src/core/migrations.rs
@tests/ingest_pipeline.rs
@tests/foundation_schema.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add schema and ingest regressions for chunk-aligned embedding persistence</name>
  <files>tests/ingest_pipeline.rs, tests/foundation_schema.rs</files>
  <read_first>
    - tests/ingest_pipeline.rs
    - tests/foundation_schema.rs
    - src/ingest/mod.rs
    - src/memory/repository.rs
    - src/core/migrations.rs
  </read_first>
  <acceptance_criteria>
    - `tests/ingest_pipeline.rs` defines `ingest_persists_chunk_aligned_embedding_sidecars`
    - `tests/ingest_pipeline.rs` defines `lexical_only_ingest_remains_usable_when_embeddings_are_disabled`
    - `tests/foundation_schema.rs` defines `embedding_foundation_schema_bootstraps_additive_vector_sidecars`
    - `cargo test --test ingest_pipeline ingest_persists_chunk_aligned_embedding_sidecars -- --nocapture` fails before production changes and passes after Task 2
  </acceptance_criteria>
  <behavior>
    - Embedding persistence must be additive and chunk-aligned.
    - Lexical-only ingest must remain usable with embeddings disabled.
    - Schema bootstrap must show embedding tables without altering authority-store ownership.
  </behavior>
  <action>Extend `tests/ingest_pipeline.rs` and `tests/foundation_schema.rs` with red-green coverage for additive embedding persistence. Add `ingest_persists_chunk_aligned_embedding_sidecars`, asserting that an ingest flow with embedding enabled creates embedding side-table rows aligned to chunk-level authority records. Add `lexical_only_ingest_remains_usable_when_embeddings_are_disabled`, asserting ingest still succeeds without embedding persistence when the backend is disabled. Add `embedding_foundation_schema_bootstraps_additive_vector_sidecars` in `tests/foundation_schema.rs`, asserting the new embedding tables and optional vector-sidecar state bootstrap additively alongside existing schema objects.</action>
  <verify>
    <automated>cargo test --test ingest_pipeline ingest_persists_chunk_aligned_embedding_sidecars -- --nocapture</automated>
  </verify>
  <done>A failing ingest/schema regression harness exists that defines the additive embedding persistence contract before production code changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add embedding schema and ingest-time persistence without breaking lexical-only ingest</name>
  <files>migrations/0006_embedding_foundation.sql, src/core/migrations.rs, src/ingest/mod.rs, src/memory/repository.rs, tests/ingest_pipeline.rs, tests/foundation_schema.rs</files>
  <read_first>
    - src/ingest/mod.rs
    - src/memory/repository.rs
    - src/core/migrations.rs
    - tests/ingest_pipeline.rs
    - tests/foundation_schema.rs
    - .planning/phases/08-embedding-backend-and-index-foundation/08-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - `migrations/0006_embedding_foundation.sql` creates additive embedding tables keyed to authority records
    - `src/core/migrations.rs` registers migration 0006
    - `src/ingest/mod.rs` persists chunk-aligned embedding side-table rows only when embedding is enabled/ready
    - lexical-only ingest remains successful when embedding persistence is disabled
    - `cargo test --test ingest_pipeline -- --nocapture` and `cargo test --test foundation_schema -- --nocapture` both exit 0
  </acceptance_criteria>
  <behavior>
    - Embedding persistence is additive, not authority-store replacement.
    - Chunk-level embedding alignment is preserved.
    - Disabled/not-ready embedding must not break lexical-only ingest.
  </behavior>
  <action>Create `migrations/0006_embedding_foundation.sql` with additive embedding tables keyed to `memory_records(id)` and any minimal vector-sidecar/index state tables needed for Phase 8 readiness inspection. Register the migration in `src/core/migrations.rs`. Extend `src/memory/repository.rs` with repository methods for inserting and listing embedding side-table rows. In `src/ingest/mod.rs`, attach embedding persistence to ingest completion so each chunk-aligned authority record can optionally receive an embedding side-table row when embedding is enabled and the backend is available; keep ingest successful and lexical-only when the backend is disabled or not ready. Update the new tests so schema bootstrap, embedding-sidecar persistence, and lexical-only ingest compatibility all pass.</action>
  <verify>
    <automated>cargo test --test ingest_pipeline -- --nocapture</automated>
  </verify>
  <done>The codebase now has additive chunk-aligned embedding persistence and optional vector-sidecar schema without harming lexical-only ingest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ingest completion -> embedding persistence | Chunked authority records now optionally trigger embedding-sidecar writes. |
| authority store -> embedding side tables | Embedding rows must remain additive and keyed to authority records without redefining ownership. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02 | T | `src/ingest/mod.rs` / `src/memory/repository.rs` | mitigate | Persist embeddings only as additive side-table rows keyed to `memory_records(id)`, keeping authority ownership and lexical-only ingest intact. |
</threat_model>

<verification>
- `cargo test --test ingest_pipeline ingest_persists_chunk_aligned_embedding_sidecars -- --nocapture`
- `cargo test --test ingest_pipeline lexical_only_ingest_remains_usable_when_embeddings_are_disabled -- --nocapture`
- `cargo test --test ingest_pipeline -- --nocapture`
- `cargo test --test foundation_schema embedding_foundation_schema_bootstraps_additive_vector_sidecars -- --nocapture`
- `cargo clippy --all-targets -- -D warnings`
</verification>

<success_criteria>
- Embeddings persist additively at chunk/retrieval-unit grain.
- Lexical-only ingest still works when embedding is disabled.
- Embedding sidecar schema exists without redefining authority storage.
</success_criteria>

<output>
After completion, create `.planning/phases/08-embedding-backend-and-index-foundation/08-02-SUMMARY.md`
</output>
