---
phase: 08-embedding-backend-and-index-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-02
files_modified:
  - src/core/status.rs
  - src/interfaces/cli.rs
  - tests/status_cli.rs
  - tests/foundation_schema.rs
autonomous: true
requirements:
  - EMB-03
  - OPS-01
must_haves:
  truths:
    - "Optional vector sidecar/index state is inspectable and does not silently masquerade as lexical readiness."
    - "`status` / `doctor` can report embedding/vector substrate truthfully without enabling dual-channel retrieval yet."
    - "Foundation inspection and diagnostics stay local-first and operator-readable."
  artifacts:
    - path: "src/core/status.rs"
      provides: "Vector-sidecar/index readiness reporting"
    - path: "src/interfaces/cli.rs"
      provides: "Operator-facing inspection output that includes embedding/vector substrate state"
    - path: "tests/status_cli.rs"
      provides: "Regression coverage for vector-sidecar readiness diagnostics"
  key_links:
    - from: "src/core/status.rs"
      to: "tests/status_cli.rs"
      via: "status/doctor output assertions for vector substrate readiness"
      pattern: "StatusReport"
---

<objective>
Finish the Phase 8 foundation by making the optional vector sidecar/index bootstrap and readiness state inspectable through the existing operator surfaces.

Purpose: ensure the embedding substrate can be diagnosed truthfully before Phase 9 starts using it for dual-channel retrieval behavior.
Output: vector-sidecar readiness diagnostics plus regression coverage.
</objective>

<execution_context>
@/home/tongyuan/.codex/get-shit-done/workflows/execute-plan.md
@/home/tongyuan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-embedding-backend-and-index-foundation/08-CONTEXT.md
@.planning/phases/08-embedding-backend-and-index-foundation/08-RESEARCH.md
@.planning/phases/08-embedding-backend-and-index-foundation/08-PATTERNS.md
@src/core/status.rs
@src/core/doctor.rs
@src/interfaces/cli.rs
@tests/status_cli.rs
@tests/foundation_schema.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add diagnostics regressions for vector sidecar/index readiness</name>
  <files>tests/status_cli.rs, tests/foundation_schema.rs</files>
  <read_first>
    - tests/status_cli.rs
    - tests/foundation_schema.rs
    - src/core/status.rs
    - src/interfaces/cli.rs
  </read_first>
  <acceptance_criteria>
    - `tests/status_cli.rs` defines `embedding_foundation_status_reports_vector_sidecar_state`
    - `tests/foundation_schema.rs` or adjacent schema coverage asserts additive vector-sidecar/index bootstrap state
    - `cargo test --test status_cli embedding_foundation_status_reports_vector_sidecar_state -- --nocapture` fails before production changes and passes after Task 2
  </acceptance_criteria>
  <behavior>
    - Status output must distinguish lexical index readiness from embedding/vector substrate readiness.
    - Operator diagnostics must stay truthful when the vector sidecar/index is missing or not built.
    - No retrieval behavior change is required yet.
  </behavior>
  <action>Extend `tests/status_cli.rs` with `embedding_foundation_status_reports_vector_sidecar_state`, asserting that status output can surface missing vs ready vector-sidecar/index state without confusing it with lexical readiness. If needed, extend `tests/foundation_schema.rs` to assert the sidecar/index bootstrap objects expected by Phase 8 exist additively. Keep the new tests focused on diagnostics and schema state, not on retrieval fusion behavior.</action>
  <verify>
    <automated>cargo test --test status_cli embedding_foundation_status_reports_vector_sidecar_state -- --nocapture</automated>
  </verify>
  <done>A failing diagnostics regression defines the operator-visible vector-sidecar state contract before production changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose vector-sidecar/index readiness through status and operator surfaces</name>
  <files>src/core/status.rs, src/interfaces/cli.rs, tests/status_cli.rs, tests/foundation_schema.rs</files>
  <read_first>
    - src/core/status.rs
    - src/core/doctor.rs
    - src/interfaces/cli.rs
    - tests/status_cli.rs
    - .planning/phases/08-embedding-backend-and-index-foundation/08-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - `src/core/status.rs` reports vector-sidecar/index readiness distinctly from lexical readiness
    - operator-facing status/inspect output surfaces embedding/vector substrate state truthfully
    - `cargo test --test status_cli -- --nocapture` exits 0
    - `cargo test --test foundation_schema -- --nocapture` exits 0
  </acceptance_criteria>
  <behavior>
    - Diagnostics stay truthful and local-first.
    - Lexical readiness remains distinct from embedding/vector readiness.
    - Phase 8 still does not enable dual-channel retrieval behavior.
  </behavior>
  <action>In `src/core/status.rs`, add distinct inspectable state for the optional embedding vector sidecar/index and include it in rendered status output without conflating it with lexical index readiness. Update `src/interfaces/cli.rs` operator-facing rendering as needed so `status` and schema inspection make the new substrate visible. Keep `doctor` semantics lexical-first and non-misleading: if the embedding substrate is absent, diagnostics may report it, but Phase 8 must not imply that dual-channel retrieval is already active. Update the new diagnostics regression so vector-sidecar readiness, lexical readiness, and embedding backend readiness are all reported clearly.</action>
  <verify>
    <automated>cargo test --test status_cli -- --nocapture</automated>
  </verify>
  <done>Operators can inspect the optional embedding/vector substrate truthfully before Phase 9 begins using it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vector-sidecar bootstrap state -> operator diagnostics | Missing or incomplete vector substrate must not be misreported as lexical readiness. |
| status/inspect output -> developer behavior | Operators rely on these diagnostics to decide whether semantic substrate setup is complete. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | R | `src/core/status.rs` / `src/interfaces/cli.rs` | mitigate | Report vector-sidecar/index readiness explicitly and separately from lexical readiness so operators cannot misinterpret the substrate state. |
</threat_model>

<verification>
- `cargo test --test status_cli embedding_foundation_status_reports_vector_sidecar_state -- --nocapture`
- `cargo test --test status_cli -- --nocapture`
- `cargo test --test foundation_schema -- --nocapture`
- `cargo clippy --all-targets -- -D warnings`
</verification>

<success_criteria>
- Vector-sidecar readiness is inspectable and distinct from lexical readiness.
- Operator surfaces remain truthful and local-first.
- Phase 8 still stops short of dual-channel retrieval behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/08-embedding-backend-and-index-foundation/08-03-SUMMARY.md`
</output>
