---
phase: 02-ingest-and-lightweight-retrieval
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - src/search/mod.rs
  - src/search/rerank.rs
  - src/search/filter.rs
  - src/search/citation.rs
  - src/interfaces/cli.rs
  - src/interfaces/mod.rs
  - tests/retrieval_cli.rs
autonomous: true
requirements: [RET-03, RET-04, RET-05, AGT-01]
must_haves:
  truths:
    - "Ordinary retrieval returns ranked results with score breakdowns, citations, scope filters, and validity metadata."
    - "Developer can use ordinary retrieval end-to-end from the CLI or library surface without invoking Rig or any LLM."
    - "Filtering by scope, record type, truth layer, and validity affects candidate selection and is visible in the result trace."
  artifacts:
    - path: "src/search/filter.rs"
      provides: "Typed filter application for scope, record type, truth layer, and validity windows."
    - path: "src/search/citation.rs"
      provides: "Structured citations and chunk anchors for explainable retrieval results."
    - path: "src/interfaces/cli.rs"
      provides: "Thin `ingest` and `search` command surface over ordinary retrieval services."
    - path: "tests/retrieval_cli.rs"
      provides: "End-to-end CLI coverage for ingest, search, filters, and explainable JSON/text results."
  key_links:
    - from: "src/search/filter.rs"
      to: "src/search/lexical.rs"
      via: "Filters restrict SQL candidate selection before rerank."
      pattern: "scope|record_type|truth_layer|valid"
    - from: "src/search/rerank.rs"
      to: "src/search/score.rs"
      via: "Final ranked results preserve typed score breakdowns instead of opaque scores."
      pattern: "final_score|ScoreBreakdown"
    - from: "src/interfaces/cli.rs"
      to: "src/search/mod.rs"
      via: "CLI delegates ingest/search requests to internal services without LLM or Rig types."
      pattern: "Ingest|Search"
---

<objective>
Finish the ordinary retrieval surface with explainable ranking, filtering, citations, and CLI/library access.

Purpose: complete the Phase 2 ordinary retrieval contract so users and downstream code can ingest and search through lexical-first retrieval with structured explanations, citations, and filters, while keeping Rig and semantic retrieval out of scope.
Output: structured retrieval result contracts, end-to-end CLI commands, and tests covering citations, filters, and explainable response shape.
</objective>

<execution_context>
@/home/tongyuan/.codex/get-shit-done/workflows/execute-plan.md
@/home/tongyuan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-ingest-and-lightweight-retrieval/02-CONTEXT.md
@.planning/phases/02-ingest-and-lightweight-retrieval/02-RESEARCH.md
@.planning/phases/02-ingest-and-lightweight-retrieval/02-PATTERNS.md
@.planning/phases/01-foundation-kernel/01-03-SUMMARY.md
@/home/tongyuan/project/agent_memos/src/interfaces/cli.rs
@/home/tongyuan/project/agent_memos/src/core/status.rs

<interfaces>
From `src/interfaces/cli.rs`:
```rust
pub enum Commands {
    Init,
    Status,
    Doctor,
    Inspect { command: InspectCommands },
}

pub fn run(cli: Cli, config: Config) -> Result<ExitCode>;
```

From `src/core/status.rs`:
```rust
pub struct StatusReport {
    pub schema_version: Option<u32>,
    pub index_readiness: CapabilityState,
    pub readiness_notes: Vec<String>,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add filtering, citations, and explainable ranked result contracts</name>
  <files>src/search/mod.rs, src/search/rerank.rs, src/search/filter.rs, src/search/citation.rs, tests/retrieval_cli.rs</files>
  <behavior>
    - Search responses expose structured citations, chunk anchors, applied filters, and a typed score breakdown for every result.
    - Scope, record type, truth layer, and validity filtering happen before or during candidate selection, not as an opaque afterthought.
    - Final ranking remains lexical-first and deterministic; no semantic merge logic, RRF, or Rig orchestration appears in this plan.
  </behavior>
  <action>Extend the `search` module with explicit request/response contracts, result trace types, `filter.rs`, `citation.rs`, and a `rerank.rs` layer that turns scored candidates into stable ordinary retrieval results, following the search/citation/test patterns mapped in `02-PATTERNS.md`. Keep lexical score as the primary explanation source, keep `valid_from` / `valid_to` as the explicit Phase 2 validity contract, and preserve future semantic coexistence only as an extension seam. `tests/retrieval_cli.rs` should exercise the library-facing ordinary retrieval path first, including filter traces and structured citation fields.</action>
  <verify>
    <automated>cargo test --test retrieval_cli library_search_returns_citations_and_filter_trace -- --nocapture</automated>
  </verify>
  <done>Library callers receive ranked ordinary retrieval results with citations, applied-filter traces, validity metadata, and stable score breakdowns.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire thin CLI commands for ingest and ordinary retrieval</name>
  <files>src/interfaces/cli.rs, src/interfaces/mod.rs, tests/retrieval_cli.rs</files>
  <behavior>
    - `agent-memos ingest ...` calls the internal ingest service and can emit concise text output or JSON reports.
    - `agent-memos search ...` calls the internal ordinary retrieval service and returns ranked results with citations and optional trace details.
    - CLI behavior remains a thin wrapper over library APIs; no CLI branch reimplements search logic or directly manipulates SQL.
    - Status/reporting semantics remain the responsibility of Plan `02-02`; this task only adds the ordinary retrieval command surface.
  </behavior>
  <action>Extend the existing clap surface with `ingest` and `search` subcommands, keeping command parsing in `src/interfaces/cli.rs` and re-export wiring in `src/interfaces/mod.rs` per the thin-CLI pattern mapped in `02-PATTERNS.md`. Support `--json`, `--top-k`, `--scope`, `--record-type`, `--truth-layer`, `--valid-at`, `--from`, `--to`, and `--trace` on the search path, plus the minimum ingest arguments needed to exercise the supported text formats from Plan `02-01`. Do not modify the canonical status/readiness contract here. Do not add HTTP, MCP, Rig, or provider-specific code.</action>
  <verify>
    <automated>cargo test --test retrieval_cli -- --nocapture</automated>
  </verify>
  <done>Developers can ingest and search through the CLI with explainable ordinary retrieval results, and the Phase 2 public surface remains a thin wrapper over the underlying library services.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args -> ordinary retrieval services | Untrusted flags and query text cross into filtering, ranking, and output rendering. |
| Retrieval service -> rendered output | Structured citations and traces become operator-visible output and must remain accurate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | S | `src/interfaces/cli.rs` | mitigate | Parse CLI options into typed request structs so unsupported values fail fast instead of silently broadening retrieval scope. |
| T-02-08 | I | `src/search/citation.rs` | mitigate | Derive citations only from persisted source/chunk metadata; never synthesize anchors from snippets alone. |
| T-02-09 | R | `src/search/rerank.rs` | mitigate | Include applied filters and `ScoreBreakdown` in the result trace so output ranking remains auditable and reproducible. |
</threat_model>

<verification>
Run the library-shape retrieval test after Task 1, then the full retrieval CLI test file after Task 2. This plan deliberately excludes status/readiness changes because the canonical status contract is already owned by Plan `02-02`.
</verification>

<success_criteria>
Phase 2 is complete when ordinary retrieval can be used end-to-end from CLI or library code without LLMs, every result carries structured citation and filter/score trace data, and scope/record/truth/validity filtering is part of the supported public contract.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ingest-and-lightweight-retrieval/02-03-SUMMARY.md`
</output>
