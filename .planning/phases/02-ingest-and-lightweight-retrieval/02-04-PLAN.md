---
phase: 02-ingest-and-lightweight-retrieval
plan: 04
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - src/core/app.rs
  - tests/status_cli.rs
autonomous: true
requirements: [RET-01]
gap_closure: true
must_haves:
  truths:
    - "An initialized `lexical_only` database reports lexical capability truthfully: status output keeps `lexical_dependency_state: ready` and `index_readiness: ready` without also claiming lexical retrieval or index creation are deferred."
    - "Semantic capability remains explicitly deferred for `embedding_only` and `hybrid`; this gap closure does not change the three retrieval modes or introduce semantic execution."
    - "The canonical status CLI regression fails if stale Phase 1 lexical-deferral text reappears in initialized `lexical_only` output."
  artifacts:
    - path: "src/core/app.rs"
      provides: "Truthful `RuntimeReadiness::from_config()` notes for `lexical_only` while preserving deferred semantic-mode notes."
    - path: "tests/status_cli.rs"
      provides: "Integration regression coverage for initialized `lexical_only` status output and stale-note rejection."
  key_links:
    - from: "src/core/app.rs"
      to: "src/core/status.rs"
      via: "`StatusReport::collect()` copies `app.readiness.notes`, so the runtime-readiness note text must match real Phase 2 lexical capability."
      pattern: "app\\.readiness\\.notes|readiness_notes"
    - from: "tests/status_cli.rs"
      to: "agent-memos status"
      via: "The integration test initializes a real SQLite database, runs the CLI, and asserts the rendered text stops claiming lexical retrieval is deferred."
      pattern: "status_exits_successfully_for_reserved_modes|index_readiness: ready"
---

<objective>
Close the single Phase 2 verification gap in status truthfulness for initialized `lexical_only` mode.

Purpose: make the user-facing readiness notes align with the already-real Phase 2 lexical implementation while preserving the deferred semantic contract for `embedding_only` and `hybrid`.
Output: corrected runtime-readiness wording plus a CLI regression that fails if initialized lexical-only status output still claims lexical retrieval or index creation are deferred.
</objective>

<execution_context>
@/home/tongyuan/.codex/get-shit-done/workflows/execute-plan.md
@/home/tongyuan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-ingest-and-lightweight-retrieval/02-CONTEXT.md
@.planning/phases/02-ingest-and-lightweight-retrieval/02-VERIFICATION.md
@.planning/phases/02-ingest-and-lightweight-retrieval/02-02-SUMMARY.md
@/home/tongyuan/project/agent_memos/src/core/app.rs
@/home/tongyuan/project/agent_memos/src/core/status.rs
@/home/tongyuan/project/agent_memos/tests/status_cli.rs

<interfaces>
From `src/core/app.rs`:
```rust
pub struct RuntimeReadiness {
    pub configured_mode: RetrievalMode,
    pub effective_mode: RetrievalMode,
    pub ready: bool,
    pub notes: Vec<String>,
}

impl RuntimeReadiness {
    pub fn from_config(config: &Config) -> Self;
}
```

From `src/core/status.rs`:
```rust
pub struct StatusReport {
    pub lexical_dependency_state: CapabilityState,
    pub index_readiness: CapabilityState,
    pub ready: bool,
    pub readiness_notes: Vec<String>,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Correct lexical-only runtime-readiness notes in the status source</name>
  <files>src/core/app.rs</files>
  <behavior>
    - `RuntimeReadiness::from_config()` keeps `lexical_only` ready after Phase 2 initialization, but its notes no longer say lexical retrieval dependency loading or index creation are deferred.
    - The replacement wording explicitly reflects the real Phase 2 lexical-first baseline and does not weaken the deferred semantic wording for `embedding_only` or `hybrid`.
    - Inline unit coverage in `src/core/app.rs` proves the lexical-only notes changed while the retrieval-mode semantics remain intact.
  </behavior>
  <action>Update `RuntimeReadiness::from_config()` in `src/core/app.rs` so the `lexical_only` branch emits truthful Phase 2 wording per the locked Phase 2 decision that lexical capability is real while semantic capability stays deferred. Preserve the three retrieval modes and their semantics exactly as implemented; do not collapse `embedding_only` or `hybrid`, do not add semantic fallbacks, and do not change readiness field computation outside the stale note text and any directly related unit assertions in this file.</action>
  <verify>
    <automated>cargo test config_runtime_readiness_preserves_semantic_intent -- --nocapture</automated>
  </verify>
  <done>`src/core/app.rs` reports truthful lexical-only notes for Phase 2 and unit coverage rejects the stale Phase 1 deferral language without changing deferred semantic-mode behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add an initialized lexical-only CLI regression for stale deferred wording</name>
  <files>tests/status_cli.rs</files>
  <behavior>
    - The canonical status CLI test still verifies the three retrieval modes, readiness fields, and success semantics.
    - The initialized `lexical_only` path now also asserts that rendered `status` output does not contain claims that lexical retrieval or index creation are deferred once the database has been initialized.
    - The regression remains focused on user-visible output truthfulness and does not broaden scope into unrelated CLI or retrieval behavior.
  </behavior>
  <action>Extend `tests/status_cli.rs` with a regression that initializes a real `lexical_only` database, runs `agent-memos status`, and fails if the output still says lexical retrieval or index creation are deferred. Keep the existing readiness-field assertions, and make the stale-note rejection explicit enough that future regressions in `src/core/app.rs` or `src/core/status.rs` fail immediately. Do not add unrelated planning-doc updates, new test files, or broader CLI assertions.</action>
  <verify>
    <automated>cargo test --test status_cli -- --nocapture</automated>
  </verify>
  <done>The canonical status CLI suite proves initialized lexical-only output is truthful about real lexical capability and still preserves deferred semantic reporting for the other modes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime readiness -> rendered status output | Internal readiness notes become operator-visible diagnostics and must not misstate actual capability. |
| CLI integration test -> public contract | Regression coverage defines the truthfulness contract for initialized status output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | I | `src/core/app.rs` | mitigate | Replace stale lexical-deferral text with Phase 2 truthful wording so diagnostics do not contradict ready lexical fields. |
| T-02-11 | R | `tests/status_cli.rs` | mitigate | Add a real init + status regression that asserts stale lexical deferral text is absent, making future contract drift observable. |
</threat_model>

<verification>
Run the focused `src/core/app.rs` unit test after correcting the lexical-only note text, then run the full `tests/status_cli.rs` suite to prove initialized lexical-only output is truthful and semantic modes still remain deferred.
</verification>

<success_criteria>
Phase 2's remaining verification gap is closed when initialized `lexical_only` status output no longer contradicts its own ready lexical fields, semantic capability remains deferred for `embedding_only` and `hybrid`, and the canonical status CLI tests fail if the stale Phase 1 note returns.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ingest-and-lightweight-retrieval/02-04-SUMMARY.md`
</output>
