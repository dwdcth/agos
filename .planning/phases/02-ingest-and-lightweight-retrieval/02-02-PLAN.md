---
phase: 02-ingest-and-lightweight-retrieval
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - Cargo.toml
  - Cargo.lock
  - migrations/0003_lexical_sidecar.sql
  - src/core/db.rs
  - src/core/migrations.rs
  - src/core/status.rs
  - src/lib.rs
  - src/search/mod.rs
  - src/search/lexical.rs
  - src/search/score.rs
  - tests/foundation_schema.rs
  - tests/lexical_search.rs
  - tests/status_cli.rs
autonomous: true
requirements: [ING-03, RET-01, RET-02]
must_haves:
  truths:
    - "Developer can run lexical recall over Chinese and PinYin-oriented text using the additive Phase 2 FTS sidecar."
    - "Rust-side lightweight score composition changes ranking in a deterministic, inspectable way without adding semantic execution."
    - "Status and readiness output become truthful about lexical capability through the canonical `tests/status_cli.rs` contract while semantic capability stays deferred."
  artifacts:
    - path: "migrations/0003_lexical_sidecar.sql"
      provides: "`libsimple`-backed external-content FTS sidecar and additive lexical schema objects."
    - path: "src/search/lexical.rs"
      provides: "FTS5/libsimple candidate recall over authority-backed chunk records."
    - path: "src/search/score.rs"
      provides: "Deterministic Rust-side scoring and `ScoreBreakdown` composition."
    - path: "tests/status_cli.rs"
      provides: "Canonical readiness regression coverage for lexical capability and deferred semantic modes."
  key_links:
    - from: "src/core/db.rs"
      to: "migrations/0003_lexical_sidecar.sql"
      via: "Database bootstrap prepares `libsimple` before the lexical migration and later FTS queries run."
      pattern: "enable_auto_extension|set_jieba_dict|apply_migrations"
    - from: "src/search/lexical.rs"
      to: "migrations/0003_lexical_sidecar.sql"
      via: "FTS queries run against the external-content sidecar and hydrate authority rows."
      pattern: "MATCH|bm25|snippet"
    - from: "src/core/status.rs"
      to: "tests/status_cli.rs"
      via: "Lexical readiness reflects the real Phase 2 capability without changing deferred semantic semantics."
      pattern: "CapabilityState::Ready|Deferred"
---

<objective>
Implement the lexical sidecar, candidate recall, and lightweight Rust scoring layer for ordinary retrieval.

Purpose: make lexical-first search real in Phase 2 by adding `libsimple` bootstrap plus the FTS sidecar, composing a deterministic Rust score breakdown, and surfacing truthful lexical readiness while keeping semantic modes deferred.
Output: a `search` module with candidate recall plus scoring, additive lexical schema/bootstrap, and tests proving lexical search works without any LLM or agent runtime.
</objective>

<execution_context>
@/home/tongyuan/.codex/get-shit-done/workflows/execute-plan.md
@/home/tongyuan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-ingest-and-lightweight-retrieval/02-CONTEXT.md
@.planning/phases/02-ingest-and-lightweight-retrieval/02-RESEARCH.md
@.planning/phases/02-ingest-and-lightweight-retrieval/02-PATTERNS.md
@.planning/phases/01-foundation-kernel/01-03-SUMMARY.md
@/home/tongyuan/project/agent_memos/src/core/db.rs
@/home/tongyuan/project/agent_memos/src/core/status.rs
@/home/tongyuan/project/agent_memos/tests/status_cli.rs

<interfaces>
From `src/core/status.rs`:
```rust
pub enum CapabilityState {
    Ready,
    Deferred,
    Disabled,
    Missing,
    NotApplicable,
    NotBuiltInPhase1,
}

pub struct StatusReport {
    pub configured_mode: RetrievalMode,
    pub lexical_dependency_state: CapabilityState,
    pub index_readiness: CapabilityState,
    pub ready: bool,
}
```

From `src/memory/record.rs`:
```rust
pub enum Scope { Project, Session, User }
pub enum RecordType { Observation, Decision, Fact }
pub enum TruthLayer { T1, T2, T3 }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add `libsimple` bootstrap and additive lexical sidecar schema</name>
  <files>Cargo.toml, Cargo.lock, migrations/0003_lexical_sidecar.sql, src/core/db.rs, src/core/migrations.rs, tests/foundation_schema.rs</files>
  <behavior>
    - `Database::open()` prepares tokenizer support once and can advance a fresh database through schema version 3 without changing the Phase 1 retrieval-mode contract.
    - `memory_records` remains the authority store while the external-content FTS sidecar, triggers, and rebuild helpers are added additively for lexical recall.
    - The lexical schema persists only the metadata needed for lightweight retrieval; it does not add semantic/vector tables or hidden fallback behavior.
    - Schema tests assert the lexical sidecar objects exist and remain rebuildable from authority rows.
  </behavior>
  <action>Add the `libsimple` dependency and connection bootstrap in `src/core/db.rs`, then register a focused `migrations/0003_lexical_sidecar.sql` from `src/core/migrations.rs` so lexical indexing lands as a separate additive seam after the ingest foundation. Keep extension loading internal and one-time only. Do not let this task redesign the authority schema, add semantic retrieval, or push readiness semantics into ad hoc CLI code.</action>
  <verify>
    <automated>cargo test --test foundation_schema -- --nocapture</automated>
  </verify>
  <done>Fresh databases reach schema version 3 with the lexical sidecar present, existing authority rows remain authoritative, and schema tests prove the additive lexical objects exist without semantic creep.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add lexical recall, Rust scoring, and canonical status readiness assertions</name>
  <files>src/core/status.rs, src/lib.rs, src/search/mod.rs, src/search/lexical.rs, src/search/score.rs, tests/lexical_search.rs, tests/status_cli.rs</files>
  <behavior>
    - Search code issues parameterized FTS5 queries against the Phase 2 sidecar and hydrates authority rows from `memory_records`.
    - A typed `ScoreBreakdown` exposes lexical base score plus lightweight bonuses such as keyword overlap, importance defaults, recency, and emotion defaults.
    - Lexical score remains the dominant term; bonuses perturb ranking but do not replace the lexical-first ordinary-retrieval contract from `02-CONTEXT.md`.
    - `tests/status_cli.rs` becomes the canonical place where lexical readiness, schema/index state, and deferred semantic modes are asserted for Phase 2.
  </behavior>
  <action>Create `src/search/mod.rs`, `src/search/lexical.rs`, and `src/search/score.rs` with a narrow candidate-recall service and deterministic Rust scoring. Export `pub mod search;` from `src/lib.rs`. Update `src/core/status.rs` so lexical readiness becomes real for Phase 2 while preserving the configured/effective mode contract from Phase 1. Move the readiness assertions into `tests/status_cli.rs`, and keep `tests/lexical_search.rs` focused on lexical recall and scoring behavior. Do not add semantic fallback behavior for `embedding_only` or `hybrid`, and do not let the search layer own CLI rendering.</action>
  <verify>
    <automated>cargo test --test lexical_search --test status_cli -- --nocapture</automated>
  </verify>
  <done>Library code can perform lexical candidate recall with an inspectable score breakdown, tests prove bonus composition is deterministic, and the canonical status CLI contract reports real lexical readiness without changing deferred semantic semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Search request -> lexical SQL | Untrusted query text and limits cross into the FTS query builder. |
| FTS candidate recall -> Rust scorer | Database-derived snippets and metadata cross into ranking logic and explanation traces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | T | `src/search/lexical.rs` | mitigate | Use parameterized SQL and explicit query helpers for FTS search terms; never interpolate raw query text into SQL strings. |
| T-02-05 | R | `src/search/score.rs` | mitigate | Keep a typed `ScoreBreakdown` so rank changes can be audited and tests can assert exact contribution sources. |
| T-02-06 | D | `src/search/lexical.rs` | mitigate | Bound recall limits and filter candidate counts before Rust-side scoring to prevent unbounded query amplification. |
</threat_model>

<verification>
Run schema tests after Task 1, then run `tests/lexical_search.rs` plus the canonical `tests/status_cli.rs` contract after Task 2. Status/readiness verification is owned by this plan and must not be deferred to the CLI-wiring plan.
</verification>

<success_criteria>
Phase 2 lexical search is complete when library callers can run `libsimple`-backed lexical recall over ingested chunks, receive deterministic Rust-side score breakdowns, and see truthful readiness output for lexical capability through `tests/status_cli.rs` without any semantic execution path.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ingest-and-lightweight-retrieval/02-02-SUMMARY.md`
</output>
