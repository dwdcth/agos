---
phase: 02-ingest-and-lightweight-retrieval
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/0002_ingest_foundation.sql
  - src/core/migrations.rs
  - src/lib.rs
  - src/memory/record.rs
  - src/memory/repository.rs
  - src/ingest/mod.rs
  - src/ingest/detect.rs
  - src/ingest/normalize.rs
  - src/ingest/chunk.rs
  - tests/foundation_schema.rs
  - tests/ingest_pipeline.rs
autonomous: true
requirements: [ING-01, ING-02]
must_haves:
  truths:
    - "Developer can ingest text input into normalized chunk records without any LLM or embedding runtime."
    - "Each persisted chunk keeps source linkage, chunk ordering, provenance anchors, and explicit validity-window fields as first-class metadata."
    - "The authority store remains `memory_records`, and Phase 2 ingest metadata lands additively before lexical recall work begins."
  artifacts:
    - path: "migrations/0002_ingest_foundation.sql"
      provides: "Phase 2 ingest schema, chunk metadata storage, and nullable `valid_from` / `valid_to` authority fields."
    - path: "src/ingest/mod.rs"
      provides: "Synchronous ingest orchestration from detect -> normalize -> chunk -> persist."
    - path: "src/memory/record.rs"
      provides: "Typed source/chunk/validity metadata used by ingest and later retrieval plans."
    - path: "tests/ingest_pipeline.rs"
      provides: "Regression coverage for normalization, chunking, and ingest persistence."
  key_links:
    - from: "src/core/migrations.rs"
      to: "migrations/0002_ingest_foundation.sql"
      via: "Phase 2 ingest schema is registered as an additive migration."
      pattern: "apply_migrations|0002_ingest_foundation"
    - from: "src/ingest/mod.rs"
      to: "src/memory/repository.rs"
      via: "IngestService persists chunk drafts into authority `memory_records` rows."
      pattern: "persist|insert"
    - from: "src/ingest/chunk.rs"
      to: "src/memory/record.rs"
      via: "ChunkDraft maps to typed chunk provenance and validity metadata."
      pattern: "chunk_index|chunk_count|anchor|valid_from|valid_to"
---

<objective>
Implement the Phase 2 ingest foundation on top of the Phase 1 single-crate kernel.

Purpose: deliver normalized source ingestion, chunk persistence, and the additive ingest metadata contract needed by later lexical retrieval work, without mixing in `libsimple` bootstrap or search concerns.
Output: a synchronous ingest pipeline plus authority-store schema/model changes for chunk provenance and nullable validity windows.
</objective>

<execution_context>
@/home/tongyuan/.codex/get-shit-done/workflows/execute-plan.md
@/home/tongyuan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-ingest-and-lightweight-retrieval/02-CONTEXT.md
@.planning/phases/02-ingest-and-lightweight-retrieval/02-RESEARCH.md
@.planning/phases/02-ingest-and-lightweight-retrieval/02-PATTERNS.md
@.planning/phases/01-foundation-kernel/01-02-SUMMARY.md
@/home/tongyuan/project/agent_memos/src/core/migrations.rs
@/home/tongyuan/project/agent_memos/src/memory/record.rs
@/home/tongyuan/project/agent_memos/src/memory/repository.rs

<interfaces>
From `src/memory/record.rs`:
```rust
pub struct MemoryRecord {
    pub id: String,
    pub source: SourceRef,
    pub timestamp: RecordTimestamp,
    pub scope: Scope,
    pub record_type: RecordType,
    pub truth_layer: TruthLayer,
    pub provenance: Provenance,
    pub content_text: String,
}

pub struct SourceRef {
    pub uri: String,
    pub kind: SourceKind,
    pub label: Option<String>,
}
```

From `src/memory/repository.rs`:
```rust
pub struct MemoryRepository<'db> {
    conn: &'db Connection,
}

impl<'db> MemoryRepository<'db> {
    pub fn new(conn: &'db Connection) -> Self;
    pub fn insert_record(&self, record: &MemoryRecord) -> Result<(), RepositoryError>;
    pub fn get_record(&self, id: &str) -> Result<Option<MemoryRecord>, RepositoryError>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add additive ingest schema and typed chunk/validity metadata</name>
  <files>migrations/0002_ingest_foundation.sql, src/core/migrations.rs, src/memory/record.rs, src/memory/repository.rs, tests/foundation_schema.rs</files>
  <behavior>
    - `apply_migrations()` can advance a fresh database to schema version 2 without rewriting Phase 1 tables or changing the Phase 1 retrieval-mode semantics.
    - `memory_records` remains the authority row for chunk text, source linkage, truth metadata, provenance, and explicit nullable `valid_from` / `valid_to` windows per the resolved research decision.
    - The migration adds only ingest-side fields and indexes needed for chunk persistence; lexical tokenizer bootstrap and FTS sidecar creation are intentionally deferred to Plan `02-02` to keep this plan bounded.
    - Existing schema tests stop asserting "Phase 1 only" and instead assert the precise additive ingest objects and columns now present.
  </behavior>
  <action>Create `migrations/0002_ingest_foundation.sql` as the additive ingest migration, following the split migration pattern and authority-store guidance mapped in `02-PATTERNS.md`. Extend the typed memory model and repository so chunk provenance, chunk ordering, anchors, content hash, and nullable validity windows are stored explicitly instead of reconstructed later, matching the locked source-linkage and explainability requirements in `02-CONTEXT.md`. Keep `memory_records` authoritative. Do not add `libsimple`, FTS SQL, semantic/vector tables, Rig types, or hybrid fallback behavior in this plan.</action>
  <verify>
    <automated>cargo test --test foundation_schema -- --nocapture</automated>
  </verify>
  <done>Schema version 2 migrates cleanly on a fresh database, authority records persist the new chunk/validity metadata, and schema tests prove the Phase 2 ingest additions are explicit and additive.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement detect-normalize-chunk-persist ingest orchestration</name>
  <files>src/lib.rs, src/ingest/mod.rs, src/ingest/detect.rs, src/ingest/normalize.rs, src/ingest/chunk.rs, tests/ingest_pipeline.rs</files>
  <behavior>
    - Plain text, note-like text, and conversation-like exports normalize into a stable `NormalizedSource` shape.
    - Chunking preserves source URI, source kind, chunk ordering, anchor/span metadata, and optional validity windows suitable for later citations and filters.
    - Ingest persistence writes one authority `memory_records` row per chunk and leaves enough structured metadata for Plan `02-02` to build the lexical sidecar without re-parsing raw sources.
    - Code paths remain synchronous and library-usable; no CLI wiring, `libsimple`, LLM calls, or Rig dependencies appear in this plan.
  </behavior>
  <action>Create `src/ingest/` as a synchronous service boundary modeled after the split and repository/service patterns mapped in `02-PATTERNS.md`, but adapted to this repository's typed `MemoryRecord` contracts. `detect.rs` should stay a pure format detector, `normalize.rs` should convert supported text inputs into a normalized source payload, `chunk.rs` should produce stable `ChunkDraft` values with chunk indices/counts and anchors, and `mod.rs` should expose an `IngestService` plus request/report types that persist via `MemoryRepository`. Export the ingest module from `src/lib.rs`. Keep the scope locked to lexical-first ordinary retrieval: do not add PDF scraping, semantic preparation, asynchronous background work, or workspace restructuring.</action>
  <verify>
    <automated>cargo test --test ingest_pipeline -- --nocapture</automated>
  </verify>
  <done>Library callers can ingest supported text inputs into persisted chunked records with explicit source linkage and provenance, and the ingest tests cover normalization, chunk boundaries, and stored metadata shape.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/library input -> ingest service | Untrusted source text, file paths, and timestamps cross into normalization and persistence. |
| ingest service -> SQLite | Normalized chunk metadata and content cross into authority storage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T | `src/ingest/normalize.rs` | mitigate | Normalize and validate source metadata before persistence; reject malformed structured inputs instead of partially storing them. |
| T-02-02 | I | `src/memory/repository.rs` | mitigate | Persist provenance, source URI, chunk anchors, and validity metadata explicitly so later retrieval does not infer or fabricate citations. |
| T-02-03 | D | `src/ingest/chunk.rs` | mitigate | Bound chunk sizing and supported formats so pathological input does not explode chunk counts or DB writes. |
</threat_model>

<verification>
Run the schema and ingest tests independently after each task. This plan is intentionally narrowed to the ingest seam: it owns authority-schema changes plus ingest orchestration, and leaves tokenizer/bootstrap, FTS sidecar work, and status readiness changes to Plan `02-02`.
</verification>

<success_criteria>
Phase 2 ingest foundation is complete when supported text inputs can be normalized and chunked into authority `memory_records` rows with explicit source linkage, chunk provenance, and nullable validity metadata, ready for later lexical indexing without re-ingest.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ingest-and-lightweight-retrieval/02-01-SUMMARY.md`
</output>
