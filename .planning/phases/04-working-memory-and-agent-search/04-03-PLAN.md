---
phase: 04-working-memory-and-agent-search
plan: 03
type: execute
wave: 3
depends_on:
  - "04-02"
files_modified:
  - Cargo.toml
  - Cargo.lock
  - src/agent/mod.rs
  - src/agent/orchestration.rs
  - src/agent/rig_adapter.rs
  - src/interfaces/cli.rs
  - src/lib.rs
  - tests/agent_search.rs
autonomous: true
requirements:
  - AGT-02
  - AGT-03
  - AGT-04
must_haves:
  truths:
    - "Developer can invoke agent search through a thin Rig-backed orchestration path that reuses internal cognition services."
    - "Agent-search output returns a structured report with citations, selected or blocked branch state, and gate diagnostics."
    - "Rig integration cannot bypass ordinary retrieval or write directly into truth storage."
  artifacts:
    - path: "src/agent/orchestration.rs"
      provides: "Internal multi-step agent-search orchestration over retrieve/assemble/score/gate services"
      exports: ["AgentSearchRequest", "AgentSearchOrchestrator", "AgentSearchReport"]
    - path: "src/agent/rig_adapter.rs"
      provides: "Thin Rig adapter over the internal orchestration service"
      exports: ["RigAgentSearchAdapter"]
    - path: "tests/agent_search.rs"
      provides: "Deterministic orchestration and boundary regression coverage"
    - path: "src/interfaces/cli.rs"
      provides: "Developer invocation surface for structured agent-search output"
  key_links:
    - from: "src/agent/orchestration.rs"
      to: "src/search/mod.rs"
      via: "ordinary retrieval service call"
      pattern: "SearchService::search"
    - from: "src/agent/orchestration.rs"
      to: "src/cognition/assembly.rs"
      via: "WorkingMemoryAssembler"
      pattern: "WorkingMemoryAssembler"
    - from: "src/agent/rig_adapter.rs"
      to: "src/agent/orchestration.rs"
      via: "Rig delegates to internal orchestrator only"
      pattern: "AgentSearchOrchestrator"
---

<objective>
Integrate Rig-based agent-search orchestration as a thin adapter over the existing retrieval, working-memory, value, and metacognition services.

Purpose: Satisfy the agent-search requirements without letting Rig own cognition logic or durable memory behavior.
Output: Async agent-search orchestration, a thin Rig adapter, and a developer invocation surface that emits structured cited reports.
</objective>

<execution_context>
@/home/tongyuan/.codex/get-shit-done/workflows/execute-plan.md
@/home/tongyuan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-working-memory-and-agent-search/04-CONTEXT.md
@.planning/phases/04-working-memory-and-agent-search/04-RESEARCH.md
@.planning/phases/04-working-memory-and-agent-search/04-PATTERNS.md
@Cargo.toml
@src/search/mod.rs
@src/memory/governance.rs
@src/cognition/assembly.rs
@src/cognition/value.rs
@src/cognition/metacog.rs
@src/interfaces/cli.rs

<interfaces>
Expected from prior plans:
```rust
pub struct WorkingMemoryAssembler;
pub struct ValueScorer;
pub struct MetacognitionService;

pub struct DecisionReport {
    pub working_memory: WorkingMemory,
    pub scored_branches: Vec<ScoredBranchReport>,
    pub gate_report: GateReport,
}
```

Existing seam from `src/search/mod.rs`:
```rust
pub struct SearchService<'db> {
    pub fn search(&self, request: &SearchRequest) -> Result<SearchResponse, SearchError>;
}
```

Existing seam from `src/memory/governance.rs`:
```rust
pub struct TruthGovernanceService<'db> {
    pub fn new(conn: &'db Connection) -> Self;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add bounded agent-search orchestration over internal services</name>
  <files>Cargo.toml, Cargo.lock, src/agent/mod.rs, src/agent/orchestration.rs, tests/agent_search.rs</files>
  <behavior>
    - Orchestration performs retrieve -> assemble -> score -> gate in order and returns a structured report.
    - The orchestration layer uses internal service ports rather than reimplementing retrieval or cognition logic.
    - No path in this task writes to truth governance or introduces semantic retrieval / rumination behavior.
  </behavior>
  <action>Add the minimal dependencies from research (`rig-core`, `tokio`, and only supporting crates actually needed) and implement `AgentSearchRequest`, `AgentSearchOrchestrator`, and `AgentSearchReport` in `src/agent/orchestration.rs`. The orchestration service must sequence existing ordinary retrieval, working-memory assembly, value scoring, and metacognitive gating, preserve citations in the returned report, and keep turn/step bounds explicit so AGT-02 stays deterministic. Add deterministic tests with fake or scripted ports proving multi-step retrieval/evidence gathering, structured report output, and the absence of truth writes, semantic retrieval, or rumination logic.</action>
  <verify>
    <automated>rtk cargo test --test agent_search orchestrator_reuses_internal_services_and_returns_structured_report -- --nocapture</automated>
  </verify>
  <done>An internal agent-search service exists that reuses Phase 2/3/4 seams, returns structured cited payloads, and has regression tests for bounded multi-step execution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wrap orchestration in a thin Rig adapter and developer invocation surface</name>
  <files>src/agent/rig_adapter.rs, src/interfaces/cli.rs, src/lib.rs, tests/agent_search.rs</files>
  <behavior>
    - Rig only maps tool/context calls to the internal orchestrator and never owns candidate generation, scoring, or veto semantics.
    - The developer-facing entrypoint emits structured cited reports, not freeform answer strings only.
    - Live Rig smoke is optional and out of the required Nyquist suite; deterministic tests stay local and do not require API keys.
  </behavior>
  <action>Implement `RigAgentSearchAdapter` in `src/agent/rig_adapter.rs` per D-10, D-11, and D-12 so Rig is orchestration-only and delegates every cognitive step to the internal service. Expose a developer invocation surface in `src/interfaces/cli.rs` and `src/lib.rs` that runs agent search and renders a structured report containing citations, branch comparisons, and gate outcomes. Provide no write tools, no direct repository mutation hooks, and no bypass path around `SearchService` or truth-governance reads, satisfying AGT-03 and AGT-04. Live Rig smoke is explicitly optional for Phase 4 because correctness is gated by deterministic local orchestration tests; if provider credentials exist later, treat live smoke as a post-plan confidence check rather than a required execution blocker.</action>
  <verify>
    <automated>rtk cargo test --test agent_search rig_adapter_stays_thin_and_never_bypasses_search_or_truth_gates -- --nocapture</automated>
  </verify>
  <done>Rig integration is present as an adapter, agent search is invokable through a developer-facing surface, and tests prove the adapter cannot bypass ordinary retrieval or truth-governance boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rig/model tooling -> internal orchestration | External agent/runtime behavior crosses into trusted local retrieval and cognition services here. |
| CLI/library caller -> structured agent-search output | Developers consume agent-search reports and must not mistake them for ungated freeform model output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | E | `src/agent/rig_adapter.rs` | mitigate | Expose only internal retrieve/assemble/score/gate tools and keep truth mutation out of the Rig-visible surface so the adapter cannot elevate into write-back authority. |
| T-04-08 | I | `src/agent/orchestration.rs` | mitigate | Preserve citations and gate diagnostics in `AgentSearchReport` so model-generated summaries cannot discard provenance or safety state. |
| T-04-09 | D | `src/agent/orchestration.rs` | mitigate | Bound orchestration steps/turns explicitly and cover them in deterministic tests so agent loops cannot expand into uncontrolled local runtime use. |
</threat_model>

<verification>
- `rtk cargo test --test agent_search -- --nocapture`
- `rtk cargo clippy --all-targets -- -D warnings`
- Required Phase 4 execution gate is the deterministic local suite above; no provider-backed live smoke is required to complete this plan.
- Optional live smoke when provider credentials are configured: `rtk cargo test --test agent_search -- --ignored live_rig_smoke_requires_provider_env`
</verification>

<success_criteria>
- Agent search is invokable through a thin Rig-backed path that delegates to internal retrieval and cognition services.
- Returned output is a structured cited report containing selected or blocked branch state plus metacognitive diagnostics.
- The Rig adapter exposes no bypass around ordinary retrieval or truth-governance boundaries and introduces no semantic retrieval or rumination scope.
</success_criteria>

<output>
After completion, create `.planning/phases/04-working-memory-and-agent-search/04-03-SUMMARY.md`
</output>
