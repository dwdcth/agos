---
phase: 04-working-memory-and-agent-search
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/cognition/mod.rs
  - src/cognition/value.rs
  - src/cognition/metacog.rs
  - src/cognition/report.rs
  - tests/value_metacog.rs
autonomous: true
requirements:
  - COG-03
  - COG-04
must_haves:
  truths:
    - "Candidate actions receive a five-dimension value vector before any comparable projected score is used."
    - "Metacognitive supervision can return `warning`, `soft_veto`, `hard_veto`, or `escalate` as typed outcomes."
    - "Warnings enrich working-memory diagnostics while veto and escalate outcomes can block or redirect selection."
  artifacts:
    - path: "src/cognition/value.rs"
      provides: "Five-dimension value modeling and linear projection"
      exports: ["ValueVector", "ValueConfig", "ProjectedScore", "ValueScorer"]
    - path: "src/cognition/metacog.rs"
      provides: "Typed metacognitive gate evaluation"
      exports: ["GateDecision", "MetacognitionService"]
    - path: "src/cognition/report.rs"
      provides: "Structured branch-scoring and gate-report payloads"
      exports: ["DecisionReport", "ScoredBranchReport", "GateReport"]
    - path: "tests/value_metacog.rs"
      provides: "Regression coverage for value projection and gate outcomes"
  key_links:
    - from: "src/cognition/value.rs"
      to: "src/cognition/working_memory.rs"
      via: "Vec<ActionBranch> -> scored branch reports"
      pattern: "ActionBranch"
    - from: "src/cognition/metacog.rs"
      to: "src/cognition/value.rs"
      via: "scored branches feed gate evaluation"
      pattern: "ProjectedScore|ScoredBranch"
    - from: "src/cognition/report.rs"
      to: "src/cognition/metacog.rs"
      via: "gate outcome + diagnostics in structured reports"
      pattern: "GateDecision"
---

<objective>
Implement explicit value scoring and metacognitive gating on top of the typed working-memory branch field.

Purpose: Make branch comparison explainable and safe before any Rig orchestration is added.
Output: Five-dimension value scoring, typed gate outcomes, and structured decision reports over candidate branches.
</objective>

<execution_context>
@/home/tongyuan/.codex/get-shit-done/workflows/execute-plan.md
@/home/tongyuan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-working-memory-and-agent-search/04-CONTEXT.md
@.planning/phases/04-working-memory-and-agent-search/04-RESEARCH.md
@.planning/phases/04-working-memory-and-agent-search/04-PATTERNS.md
@doc/0415-价值层.md
@doc/0415-元认知层.md
@src/cognition/working_memory.rs
@src/cognition/action.rs

<interfaces>
Expected from `04-01`:
```rust
pub struct WorkingMemory {
    pub present: PresentFrame,
    pub branches: Vec<ActionBranch>,
}

pub struct PresentFrame {
    pub world_fragments: Vec<WorldFragment>,
    pub self_state: SelfState,
    pub active_goal: ActiveGoal,
    pub active_risks: Vec<RiskMarker>,
    pub metacog_flags: Vec<MetacogFlag>,
}

pub enum ActionKind {
    Epistemic,
    Instrumental,
    Regulative,
}

pub struct ActionBranch {
    pub branch_id: String,
    pub candidate: ActionCandidate,
    pub supporting_evidence: Vec<EvidenceRef>,
    pub risk_markers: Vec<RiskMarker>,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add five-dimension value scoring with dynamic weight snapshots</name>
  <files>src/cognition/mod.rs, src/cognition/value.rs, tests/value_metacog.rs</files>
  <behavior>
    - Each branch gets per-dimension scores for goal progress, information gain, risk avoidance, resource efficiency, and agent robustness.
    - Final comparison is a linear weighted combination driven by runtime `ValueConfig`.
    - Weight changes affect projected scores without changing the underlying branch or action kind contracts.
  </behavior>
  <action>Create `ValueVector`, `ProjectedScore`, `ValueConfig`, and `ValueScorer` as pure cognition transforms per D-06, D-07, D-08, and D-09. Keep the five dimensions explicit and normalized, store the exact weight snapshot used for each projection, and compare `epistemic`, `instrumental`, and `regulative` branches inside one common scoring surface. Do not add threshold blocking, semantic retrieval heuristics, or hand-wavy scalar-only scoring; blocking remains a metacognitive concern in Task 2. Add focused tests for dimension-level math, dynamic config changes, and branch comparability across the three fixed action classes.</action>
  <verify>
    <automated>rtk cargo test --test value_metacog value_scorer_projects_five_dimensions_with_dynamic_weights -- --nocapture</automated>
  </verify>
  <done>Value scoring exposes vector-first, projection-second behavior with dynamic weights and deterministic regression coverage for all five required dimensions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement typed metacognitive gates and decision reports</name>
  <files>src/cognition/metacog.rs, src/cognition/report.rs, tests/value_metacog.rs</files>
  <behavior>
    - `warning` enriches risks and metacognitive flags but does not block output.
    - `soft_veto` forces a regulating branch insertion or reselection path.
    - `hard_veto` blocks branch selection and returns a safe-response report; `escalate` pauses autonomy for human intervention.
  </behavior>
  <action>Add `MetacognitionService`, `GateDecision`, and structured `DecisionReport` / `GateReport` payloads per D-13, D-14, and D-15. Feed the service with scored branches plus working-memory state, and keep it downstream of value scoring and upstream of any agent-facing output. `warning` must annotate diagnostics only, `soft_veto` must record the rejected branch and the inserted or forced `regulative` alternative, `hard_veto` must emit a predefined safe-response path, and `escalate` must produce a paused report rather than silently downgrading. Do not write into truth governance, mutate retrieval, or collapse these outcomes into log strings.</action>
  <verify>
    <automated>rtk cargo test --test value_metacog metacog_gates_warn_veto_and_escalate_with_typed_reports -- --nocapture</automated>
  </verify>
  <done>Decision reports carry scored branches, gate diagnostics, and deterministic veto/escalation behavior that can be consumed by a later orchestration layer without hidden logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working-memory branches -> value scorer | Candidate branches from retrieval/assembly enter the value projection path here. |
| scored branches -> metacognitive gate | High-score branches must still pass safety/reliability supervision before becoming selectable outputs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | T | `src/cognition/value.rs` | mitigate | Keep `ValueConfig` typed and store per-decision weight snapshots so score tampering or silent weight drift is visible in reports and tests. |
| T-04-05 | E | `src/cognition/metacog.rs` | mitigate | Encode `warning`, `soft_veto`, `hard_veto`, and `escalate` as separate enums and reports so a high branch score cannot bypass the gate via boolean flattening. |
| T-04-06 | R | `src/cognition/report.rs` | mitigate | Persist the exact scored-branch and gate diagnostics in structured report DTOs so operator review can reconstruct why a branch was selected, vetoed, or escalated. |
</threat_model>

<verification>
- `rtk cargo test --test value_metacog -- --nocapture`
- `rtk cargo clippy --all-targets -- -D warnings`
</verification>

<success_criteria>
- Every candidate branch can be projected from a five-dimension value vector through runtime `ValueConfig`.
- Metacognition returns one of the four locked outcomes and keeps their behavior boundaries distinct.
- Structured reports expose scored branches plus gate diagnostics without mutating truth stores or retrieval services.
</success_criteria>

<output>
After completion, create `.planning/phases/04-working-memory-and-agent-search/04-02-SUMMARY.md`
</output>
