---
phase: 04-working-memory-and-agent-search
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cognition/mod.rs
  - src/cognition/action.rs
  - src/cognition/working_memory.rs
  - src/cognition/assembly.rs
  - src/lib.rs
  - tests/working_memory_assembly.rs
autonomous: true
requirements:
  - COG-01
  - COG-02
must_haves:
  truths:
    - "Working-memory assembly returns an immutable `WorkingMemory { present, branches }` object instead of a raw retrieval list."
    - "The Phase 4 action field compares `epistemic`, `instrumental`, and `regulative` branches in one typed branch list."
    - "Runtime working memory remains in memory; any trace output is explicitly debug-only and not the execution substrate."
  artifacts:
    - path: "src/cognition/working_memory.rs"
      provides: "Immutable working-memory, present-frame, and builder contracts"
      exports: ["WorkingMemory", "PresentFrame", "WorkingMemoryBuilder"]
    - path: "src/cognition/action.rs"
      provides: "Phase 4 action-kind and branch typing"
      exports: ["ActionKind", "ActionCandidate", "ActionBranch"]
    - path: "src/cognition/assembly.rs"
      provides: "Typed assembly service over existing retrieval, truth, and minimal self-state seams"
      exports: ["WorkingMemoryRequest", "SelfStateProvider", "WorkingMemoryAssembler"]
    - path: "tests/working_memory_assembly.rs"
      provides: "Regression coverage for builder assembly, branch typing, and in-memory-only runtime behavior"
  key_links:
    - from: "src/cognition/assembly.rs"
      to: "src/search/mod.rs"
      via: "SearchService::search"
      pattern: "SearchService::search"
    - from: "src/cognition/assembly.rs"
      to: "src/memory/truth.rs"
      via: "TruthRecord projections"
      pattern: "TruthRecord"
    - from: "src/cognition/working_memory.rs"
      to: "src/cognition/action.rs"
      via: "Vec<ActionBranch>"
      pattern: "branches"
---

<objective>
Implement the Phase 4 typed working-memory foundation and assembly service.

Purpose: Land the immutable cognitive control-field contract before adding value, gates, or Rig.
Output: `cognition` module scaffolding, immutable `WorkingMemory { present, branches }` types, and an assembler that consumes existing cited retrieval/truth outputs.
</objective>

<execution_context>
@/home/tongyuan/.codex/get-shit-done/workflows/execute-plan.md
@/home/tongyuan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-working-memory-and-agent-search/04-CONTEXT.md
@.planning/phases/04-working-memory-and-agent-search/04-RESEARCH.md
@.planning/phases/04-working-memory-and-agent-search/04-PATTERNS.md
@src/search/mod.rs
@src/memory/truth.rs
@src/memory/repository.rs

<interfaces>
From `src/search/mod.rs`:
```rust
pub struct SearchRequest {
    pub query: String,
    pub limit: usize,
    pub filters: SearchFilters,
}

pub struct SearchResponse {
    pub applied_filters: AppliedFilters,
    pub results: Vec<SearchResult>,
}

pub struct SearchResult {
    pub record: MemoryRecord,
    pub snippet: String,
    pub citation: Citation,
    pub score: ScoreBreakdown,
    pub trace: ResultTrace,
}

pub struct SearchService<'db> {
    pub fn search(&self, request: &SearchRequest) -> Result<SearchResponse, SearchError>;
}
```

From `src/memory/truth.rs`:
```rust
pub enum TruthRecord {
    T1 { base: MemoryRecord },
    T2 { base: MemoryRecord, open_candidates: Vec<OntologyCandidate> },
    T3 { base: MemoryRecord, t3_state: Option<T3State>, open_reviews: Vec<PromotionReview> },
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define immutable working-memory and action contracts</name>
  <files>src/cognition/mod.rs, src/cognition/action.rs, src/cognition/working_memory.rs, src/lib.rs, tests/working_memory_assembly.rs</files>
  <behavior>
    - `WorkingMemoryBuilder` refuses to build without a present frame.
    - `WorkingMemory` exposes `present` and `branches` as immutable data, not mutable runtime state.
    - `ActionKind` uses only `epistemic`, `instrumental`, and `regulative` labels even where older docs still say `operational` or `regulatory`.
  </behavior>
  <action>Create the new `cognition` domain boundary per D-01 and D-02, export it from `src/lib.rs`, and define strict typed contracts for `PresentFrame`, `WorkingMemory`, `WorkingMemoryBuilder`, `ActionCandidate`, `ActionBranch`, and `ActionKind`. Normalize Phase 4 terminology to `instrumental` / `regulative` per D-04 and D-05; do not expose the older roadmap wording in public enums or test fixtures. Keep runtime state in-memory only per D-03, so this task must not add SQLite tables, repository writes, or JSON-blob persistence. Seed focused regression tests for builder completeness, branch typing, and terminology drift.</action>
  <verify>
    <automated>rtk cargo test --test working_memory_assembly working_memory_builder_requires_present_frame_and_uses_phase4_action_labels -- --nocapture</automated>
  </verify>
  <done>`cognition` compiles as a new public module, the builder contract is explicit and immutable, and tests prove the branch field uses the locked Phase 4 action labels.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement attention-to-working-memory assembly over retrieval and truth seams</name>
  <files>src/cognition/assembly.rs, src/cognition/working_memory.rs, tests/working_memory_assembly.rs</files>
  <behavior>
    - Assembly reuses `SearchService` and typed truth projections instead of re-querying SQL rows.
    - The assembled `present` frame carries world fragments, self state, active goal, active risks, and metacognitive flags.
    - `self_state` comes from a minimal provider seam that combines runtime request context with selected truth-layer facts, not from a new durable self-model subsystem.
    - Rebuilding a fresh working-memory frame is the update path; the service does not mutate an existing frame in place.
  </behavior>
  <action>Add `WorkingMemoryRequest`, evidence-fragment DTOs, a minimal `SelfStateProvider`, and `WorkingMemoryAssembler` that consumes cited `SearchResponse` values plus existing truth/governance read models and returns `WorkingMemory { present, branches }`. The chosen Phase 4 `self_state` shape is a typed runtime snapshot assembled from request-local task context, agent capability/readiness flags, and selected truth/governance facts; do not add new SQLite tables, durable self-model storage, or a separate self-model subsystem. Preserve Phase 2 citations, traceability, and truth-layer metadata inside world fragments and branch evidence; do not recreate lexical ranking, filtering, or governance logic. Per D-03, keep the runtime object entirely in memory and limit any trace support to debug DTOs or logging only. Extend tests to prove the assembler preserves citations, carries truth context into the present frame, assembles `self_state` through the provider seam, and rebuilds fresh working-memory values rather than treating working memory as mutable storage.</action>
  <verify>
    <automated>rtk cargo test --test working_memory_assembly assembler_preserves_citations_truth_context_and_in_memory_runtime_only -- --nocapture</automated>
  </verify>
  <done>Assembly can turn ordinary retrieval plus truth-layer reads into a typed working-memory object with cited evidence, shared branch shape, and no new persistence dependency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| retrieval/truth services -> cognition assembly | Untrusted retrieved content and revocable truth metadata enter the working-memory control field here. |
| cognition runtime -> debug trace output | Debug snapshots can leak into operator tooling if trace-only state is mistaken for authoritative memory. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | T | `src/cognition/assembly.rs` | mitigate | Preserve Phase 2 citations, truth-layer tags, and validity metadata on fragments so retrieved snippets cannot be mistaken for authoritative facts without provenance. |
| T-04-02 | I | `src/cognition/working_memory.rs` | mitigate | Keep working memory runtime-only per D-03 and make any trace DTO/debug output explicitly non-authoritative, with no repository write path in this plan. |
| T-04-03 | R | `tests/working_memory_assembly.rs` | mitigate | Add deterministic tests proving builder invariants and terminology normalization so executor regressions are caught by automation instead of hidden in prose. |
</threat_model>

<verification>
- `rtk cargo test --test working_memory_assembly -- --nocapture`
- `rtk cargo clippy --all-targets -- -D warnings`
</verification>

<success_criteria>
- `WorkingMemory { present, branches }` exists as a public immutable Rust contract with builder-style assembly.
- `self_state` is populated through a minimal `SelfStateProvider` seam that combines request-local task context, agent capability/readiness flags, and selected truth/governance facts instead of a new persistent self-model store.
- `branches` can hold `epistemic`, `instrumental`, and `regulative` candidates in one typed list with cited supporting evidence.
- Assembly consumes existing retrieval/truth seams and does not add new persistence as an execution dependency.
</success_criteria>

<output>
After completion, create `.planning/phases/04-working-memory-and-agent-search/04-01-SUMMARY.md`
</output>
