---
phase: 01-foundation-kernel
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/status.rs
  - src/interfaces/cli.rs
  - tests/status_cli.rs
autonomous: true
requirements:
  - FND-03
gap_closure: true
must_haves:
  truths:
    - "Developer can run `status` at any time and get a successful report containing configured mode, effective mode, schema state, dependency loading state, index readiness state, and overall readiness, even when `db_path` points at a corrupted or non-SQLite file."
    - "Developer can run startup and inspection commands without an LLM and see truthful post-init readiness output with no stale pre-init schema-missing warning after successful initialization."
  artifacts:
    - path: "src/core/status.rs"
      provides: "Status collection that downgrades non-SQLite inspection failures into explicit non-ready report fields and notes instead of bubbling an error"
      exports:
        - "StatusReport"
    - path: "src/interfaces/cli.rs"
      provides: "CLI init/status dispatch that preserves informational `status` semantics and prints post-init-truthful output"
      exports:
        - "run"
    - path: "tests/status_cli.rs"
      provides: "Integration regressions for corrupted-db `status` behavior and truthful `init` output"
      contains: "status_reports_non_sqlite_db_as_not_ready"
  key_links:
    - from: "src/interfaces/cli.rs"
      to: "src/core/status.rs"
      via: "status and init command status collection"
      pattern: "StatusReport::collect"
    - from: "tests/status_cli.rs"
      to: "src/interfaces/cli.rs"
      via: "binary command regression coverage"
      pattern: "run_cli\\(&config_path, \\[\"status\"\\]\\)|run_cli\\(&config_path, \\[\"init\"\\]\\)"
---

<objective>
Close the two Phase 1 verification gaps in the CLI diagnostics path: `status` must stay informational for a bad on-disk database file, and `init` must stop printing stale pre-init warnings after success.

Purpose: Restore the Phase 1 `FND-03` contract so status/inspection output remains truthful, non-panicking, and command-path-sensitive without weakening the three-mode retrieval contract or doctor/init blocking rules.
Output: Hardened `StatusReport` collection, truthful `init` rendering, and integration tests covering corrupted-db status behavior plus post-init output truthfulness.
</objective>

<execution_context>
@/home/tongyuan/.codex/get-shit-done/workflows/execute-plan.md
@/home/tongyuan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundation-kernel/01-CONTEXT.md
@.planning/phases/01-foundation-kernel/01-RESEARCH.md
@.planning/phases/01-foundation-kernel/01-VERIFICATION.md
@.planning/phases/01-foundation-kernel/01-03-SUMMARY.md
@src/core/status.rs
@src/interfaces/cli.rs
@tests/status_cli.rs

Decision trace used in this plan:
- `D-03`: Keep retrieval strategy as the three explicit modes `lexical_only`, `embedding_only`, and `hybrid`.
- `D-04`: Keep embedding optional in foundation work.
- `D-05`: Startup and status checks must treat all three retrieval modes as valid states and explain what is active, unavailable, or deferred.
- `D-07`: Preserve the future coexistence rule where lexical remains the baseline explanation source in hybrid mode.

<interfaces>
From `src/core/status.rs`:
```rust
pub struct StatusReport {
    pub db_path: PathBuf,
    pub schema_version: Option<u32>,
    pub configured_mode: RetrievalMode,
    pub effective_mode: RetrievalMode,
    pub embedding_backend: EmbeddingBackend,
    pub schema_state: CapabilityState,
    pub lexical_dependency_state: CapabilityState,
    pub embedding_dependency_state: CapabilityState,
    pub base_table_state: CapabilityState,
    pub index_readiness: CapabilityState,
    pub ready: bool,
    pub readiness_notes: Vec<String>,
}

impl StatusReport {
    pub fn collect(app: &AppContext) -> Result<Self>;
    pub fn render_text(&self) -> String;
}
```

From `src/interfaces/cli.rs`:
```rust
fn init_command(app: &AppContext) -> Result<ExitCode>;
fn status_command(app: &AppContext) -> Result<ExitCode>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Downgrade unreadable database inspection failures into explicit status output</name>
  <files>src/core/status.rs, src/interfaces/cli.rs, tests/status_cli.rs</files>
  <behavior>
    - Test 1: When `db_path` points at an existing non-SQLite or corrupted file, `status` exits `0` and still prints `configured_mode`, `effective_mode`, `schema_state`, `index_readiness`, and `ready: false`.
    - Test 2: The rendered output includes an explicit note explaining that schema inspection failed for the configured file instead of surfacing a top-level command error.
    - Test 3: Reserved retrieval modes remain reported exactly as configured; the fix does not coerce `embedding_only` or `hybrid` into `lexical_only`.
  </behavior>
  <action>Update the `StatusReport::collect` path so database open / `PRAGMA user_version` / `sqlite_master` failures for an existing `db_path` are converted into a reportable non-ready state instead of bubbling an `anyhow` error. Keep `status` informational and exit-0 per `D-05`, with `ready=false`, explicit schema/base-table readiness states, and a note carrying the failure detail for operators. Preserve the existing three-mode retrieval contract from `D-03`/`D-07`: this gap fix must not change how `lexical_only`, `embedding_only`, or `hybrid` are labeled or how dependency/index readiness is reported for reserved modes. Add integration coverage in `tests/status_cli.rs` for a configured path that exists but contains non-SQLite bytes.</action>
  <verify>
    <automated>cargo test --test status_cli status_reports_non_sqlite_db_as_not_ready -- --nocapture</automated>
  </verify>
  <done>`status` never fails hard for a bad on-disk database file; it returns exit code `0`, reports explicit non-ready schema/base-table state, and explains the inspection failure in output while leaving retrieval-mode semantics unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Recompute init output after successful bootstrap so warnings stay truthful</name>
  <files>src/interfaces/cli.rs, tests/status_cli.rs</files>
  <behavior>
    - Test 1: Successful `init` on a valid `lexical_only` configuration prints `initialized: true` and `schema_version: 1` without claiming that the database schema is still missing.
    - Test 2: `init` still exits non-zero only for invalid or impossible command-path requests; reserved but deferred modes remain informational warnings, not blockers.
    - Test 3: Post-init output reflects the post-bootstrap state, not the pre-open doctor snapshot.
  </behavior>
  <action>Change `init_command` so it does not print stale pre-init warnings after `Database::open` has created or migrated the schema. After the preflight doctor gate passes and the database opens successfully, recompute the status/doctor view from the new on-disk state or otherwise filter out warnings that no longer apply. Preserve the Phase 1 rule that `doctor` and `init` block only invalid or impossible command-path requests, and keep any still-true reserved-mode warnings visible. Extend `tests/status_cli.rs` with an assertion that successful `init` output does not contain the old `database schema is not initialized yet` warning.</action>
  <verify>
    <automated>cargo test --test status_cli init_output_is_truthful_after_successful_bootstrap -- --nocapture</automated>
  </verify>
  <done>Successful `init` output is post-bootstrap truthful, no longer repeats stale schema-missing warnings, and still preserves the command-path-sensitive blocking policy for invalid runtime requests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local filesystem state -> CLI diagnostics | Arbitrary files at `db_path` become operator-facing readiness output and must not crash or misreport system state |
| preflight diagnostics -> post-init output | The command mutates SQLite state during `init`, so pre-open warnings must not be presented as post-open truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | D | `src/core/status.rs` | mitigate | Convert invalid database inspection failures into structured non-ready report fields and notes so `status` remains available even when the file is corrupted or non-SQLite. |
| T-01-11 | T | `src/interfaces/cli.rs` | mitigate | Recompute or filter warnings after `Database::open` so post-init output reflects the actual schema state rather than stale pre-init assumptions. |
| T-01-12 | R | `tests/status_cli.rs` | mitigate | Add command-level regressions that prove bad-db `status` stays informational and successful `init` output is truthful, preventing silent reintroduction of either gap. |
</threat_model>

<verification>
Run `cargo test --test status_cli -- --nocapture`.
</verification>

<success_criteria>
Phase 1 gap closure is complete when `status` always exits `0` and renders a readable non-ready report for a corrupted/non-SQLite database path, and when successful `init` output no longer prints the stale schema-missing warning while retaining the existing retrieval-mode and doctor/init blocking contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-kernel/01-04-SUMMARY.md`
</output>
