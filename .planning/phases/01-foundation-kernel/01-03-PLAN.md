---
phase: 01-foundation-kernel
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/interfaces/cli.rs
  - src/main.rs
  - src/core/app.rs
  - src/core/status.rs
  - src/core/doctor.rs
  - tests/status_cli.rs
autonomous: true
requirements:
  - FND-01
  - FND-03
must_haves:
  truths:
    - "Developer can run `status` at any time and get a successful report containing configured mode, effective mode, schema state, and overall readiness."
    - "Reserved Phase 1 retrieval modes (`embedding_only` and embedding-backed `hybrid`) are surfaced as non-ready or unimplemented in diagnostics instead of being silently coerced to lexical_only."
    - "Developer can run startup and inspection commands without an LLM and see why the local database/runtime is or is not ready."
  artifacts:
    - path: "src/core/status.rs"
      provides: "Status snapshot and renderer with configured/effective mode reporting"
      exports:
        - "StatusReport"
    - path: "src/core/doctor.rs"
      provides: "Blocking diagnostics and failure policy for invalid or impossible mode/backend combinations"
      exports:
        - "DoctorReport"
    - path: "src/interfaces/cli.rs"
      provides: "CLI surface for init, status, doctor, and inspection commands"
      exports:
        - "Cli"
        - "Commands"
    - path: "tests/status_cli.rs"
      provides: "Command-level regression coverage for status and doctor semantics"
      contains: "status_exits_successfully_for_reserved_modes"
  key_links:
    - from: "src/interfaces/cli.rs"
      to: "src/core/status.rs"
      via: "status command dispatch"
      pattern: "Commands::Status|StatusReport"
    - from: "src/interfaces/cli.rs"
      to: "src/core/doctor.rs"
      via: "doctor command dispatch"
      pattern: "Commands::Doctor|DoctorReport"
    - from: "src/core/app.rs"
      to: "src/core/db.rs"
      via: "startup validation combines config and schema state"
      pattern: "schema_version|ready"
---

<objective>
Add startup diagnostics, status reporting, and developer inspection commands on top of the foundation config and SQLite layers.

Purpose: Finish Phase 1 with a usable local-first status surface that reports readiness honestly, preserves the semantics of the three retrieval modes, and never silently downgrades unsupported reserved modes.
Output: `init`, `status`, `doctor`, and lightweight inspection commands backed by typed status/diagnostic reports and command-level tests.
</objective>

<execution_context>
@/home/tongyuan/.codex/get-shit-done/workflows/execute-plan.md
@/home/tongyuan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-kernel/01-CONTEXT.md
@.planning/phases/01-foundation-kernel/01-RESEARCH.md
@.planning/phases/01-foundation-kernel/01-PATTERNS.md
@.planning/phases/01-foundation-kernel/01-01-PLAN.md
@.planning/phases/01-foundation-kernel/01-02-PLAN.md

Decision trace used in this plan:
- `D-01`: Use TOML as the primary local config format.
- `D-03`: Model retrieval strategy as three explicit modes: `lexical_only`, `embedding_only`, `hybrid`.
- `D-04`: Keep embedding optional in foundation work.
- `D-05`: Treat all three retrieval modes as first-class states in startup/status contracts.
- `D-06`: Reserve clean extension seams for future embedding backends without making them critical-path dependencies.
- `D-07`: Preserve later lexical-plus-embedding coexistence, with lexical remaining the primary explanation source.
- `D-08`: `lexical_only` means precise, source-aware, strongly explainable retrieval.
- `D-09`: `embedding_only` means semantic-heavy recall with weaker exact explainability.
- `D-10`: `hybrid` means mixed-mode retrieval where lexical stays primary and embedding is secondary expansion/rerank.

<interfaces>
Extend the contracts created earlier with explicit status/diagnostic types:

From `src/core/status.rs`:
```rust
pub struct StatusReport {
    pub db_path: PathBuf,
    pub schema_version: Option<u32>,
    pub configured_mode: RetrievalMode,
    pub effective_mode: RetrievalMode,
    pub embedding_backend: EmbeddingBackend,
    pub ready: bool,
    pub readiness_notes: Vec<String>,
}
```

From `src/core/doctor.rs`:
```rust
pub struct DoctorReport {
    pub ready: bool,
    pub failures: Vec<String>,
    pub warnings: Vec<String>,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement startup validation and explicit readiness diagnostics</name>
  <files>src/core/app.rs, src/core/status.rs, src/core/doctor.rs, tests/status_cli.rs</files>
  <behavior>
    - Test 1: `status` always returns exit code `0` and reports `configured_mode`, `effective_mode`, and `ready` for `lexical_only`, `embedding_only`, and `hybrid`.
    - Test 2: `embedding_only` and embedding-backed `hybrid` remain visible as reserved/unimplemented in Phase 1 diagnostics; they are not silently rewritten to `lexical_only`.
    - Test 3: Invalid or impossible mode/backend combinations fail fast for `init` or startup validation on the affected command path.
  </behavior>
  <action>Build the Phase 1 readiness engine on top of the config and DB layers. Make `status` informational and always successful per the explicit planning decision: it must report configured mode, effective mode, embedding backend state, schema version, and readiness notes even when the system is non-ready. Separate this from `doctor`, which should return a blocking diagnostic result for invalid or impossible command-path initialization. Preserve the semantic meaning of each mode: `lexical_only` is the explainable baseline, `embedding_only` is semantic-heavy but unimplemented in Phase 1, and `hybrid` keeps lexical as the primary explanation source with embedding as secondary expansion/rerank. Never silently downgrade any reserved mode to `lexical_only`.</action>
  <verify>
    <automated>cargo test --test status_cli status_exits_successfully_for_reserved_modes -- --nocapture</automated>
  </verify>
  <done>Status and doctor use the same typed readiness model, reserved modes are surfaced honestly, and startup validation only fails when the requested mode/backend combination is invalid for the active command path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire CLI commands for init, status, doctor, and foundation inspection</name>
  <files>src/interfaces/cli.rs, src/main.rs, tests/status_cli.rs</files>
  <behavior>
    - Test 1: `init` opens or creates the SQLite database, applies migrations, and exits non-zero only for invalid/impossible runtime requests.
    - Test 2: `status` prints human-readable readiness details without requiring a pre-existing populated corpus.
    - Test 3: Inspection commands expose schema version and base table readiness without pretending retrieval indexes already exist in Phase 1.
  </behavior>
  <action>Create the actual Phase 1 command surface in `src/interfaces/cli.rs` and keep `src/main.rs` as a thin entrypoint. The command set should include `init`, `status`, and `doctor`, plus one lightweight inspection path such as `inspect schema` or `inspect db` that lets developers verify schema version and base table presence without requiring ingest data. Make the output honest about what Phase 1 does not implement yet: no lexical retrieval, no embedding backend, no vector index, no Rig orchestration. If the inspection output mentions retrieval readiness, phrase it as reserved seams or future capability rather than claimed functionality.</action>
  <verify>
    <automated>cargo test --test status_cli -- --nocapture</automated>
  </verify>
  <done>The binary exposes a usable Phase 1 operational surface for initialization, health/status checks, and basic schema inspection, with correct exit semantics and no silent capability downgrades.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime state -> CLI output | Internal readiness data becomes operator-facing diagnostics and must not misrepresent system capability |
| CLI command -> startup validation | User-selected command path determines whether non-ready states are informational or blocking |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | S | `src/core/status.rs` | mitigate | Report both `configured_mode` and `effective_mode` explicitly so the CLI cannot spoof readiness by collapsing reserved modes into lexical_only. |
| T-01-08 | R | `src/core/doctor.rs` | mitigate | Emit structured failure and warning messages for invalid/impossible command-path initialization so operators can audit why startup failed. |
| T-01-09 | I | `src/interfaces/cli.rs` | accept | Status/inspection output exposes only local path, schema version, and readiness metadata in Phase 1; no secrets should be printed because embedding credentials are not implemented. |
</threat_model>

<verification>
Run `cargo test --test status_cli -- --nocapture`. Then manually spot-check `cargo run -- status` and one inspection command to confirm the human-readable output matches the typed report semantics.
</verification>

<success_criteria>
Phase 1 ends with a truthful CLI status surface: `status` always succeeds, `doctor` blocks invalid/impossible startup requests, and developers can inspect database/schema readiness without any LLM or future retrieval subsystem.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-kernel/01-03-SUMMARY.md`
</output>
