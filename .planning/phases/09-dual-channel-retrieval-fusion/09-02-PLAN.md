---
phase: 09-dual-channel-retrieval-fusion
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - src/search/mod.rs
  - src/search/lexical.rs
  - src/search/score.rs
  - src/search/rerank.rs
  - src/memory/repository.rs
  - tests/dual_channel_retrieval.rs
  - tests/lexical_search.rs
autonomous: true
requirements:
  - DCR-01
  - DCR-02
  - DCR-03
must_haves:
  truths:
    - "Ordinary retrieval can run lexical-only, embedding-only, and hybrid modes through one `SearchService` seam."
    - "Hybrid retrieval dedupes lexical and embedding candidates by record identity before final ranking."
    - "Lexical remains the primary explanation surface even when embedding contributes candidates or rerank signal."
  artifacts:
    - path: "src/search/mod.rs"
      provides: "dual-channel mode dispatch through the shared retrieval service"
    - path: "src/search/lexical.rs"
      provides: "lexical candidate path preserved as the baseline recall channel"
    - path: "tests/dual_channel_retrieval.rs"
      provides: "regressions for lexical-only, embedding-only, and hybrid behavior"
  key_links:
    - from: "src/search/mod.rs"
      to: "src/memory/repository.rs"
      via: "embedding side-table reads for vector recall"
      pattern: "record_embeddings"
    - from: "src/search/mod.rs"
      to: "src/search/rerank.rs"
      via: "merged candidate set feeds final response shaping"
      pattern: "SearchResponse"
---

<objective>
Implement the actual dual-channel retrieval behavior: lexical-only, embedding-only, and hybrid recall/fusion through the shared ordinary retrieval service.

Purpose: turn the Phase 8 substrate into live dual-channel retrieval while preserving lexical-first semantics and one stable search contract.
Output: shared SearchService mode dispatch, candidate dedupe/fusion, and regression coverage across the three generated configs.
</objective>

<execution_context>
@/home/tongyuan/.codex/get-shit-done/workflows/execute-plan.md
@/home/tongyuan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-dual-channel-retrieval-fusion/09-CONTEXT.md
@.planning/phases/09-dual-channel-retrieval-fusion/09-RESEARCH.md
@.planning/phases/09-dual-channel-retrieval-fusion/09-PATTERNS.md
@config.toml
@src/search/mod.rs
@src/search/lexical.rs
@src/search/score.rs
@src/search/rerank.rs
@src/memory/repository.rs
@tests/dual_channel_retrieval.rs
@tests/lexical_search.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add retrieval regressions for lexical-only, embedding-only, and hybrid fusion behavior</name>
  <files>tests/dual_channel_retrieval.rs, tests/lexical_search.rs</files>
  <read_first>
    - tests/dual_channel_retrieval.rs
    - tests/lexical_search.rs
    - src/search/mod.rs
    - src/search/lexical.rs
    - src/search/rerank.rs
    - src/memory/repository.rs
  </read_first>
  <acceptance_criteria>
    - `tests/dual_channel_retrieval.rs` defines `mode_specific_search_behaviors_match_generated_configs`
    - `tests/dual_channel_retrieval.rs` defines `hybrid_search_merges_lexical_and_embedding_candidates_by_record_identity`
    - `cargo test --test dual_channel_retrieval hybrid_search_merges_lexical_and_embedding_candidates_by_record_identity -- --nocapture` fails before production changes and passes after Task 2
    - the new tests assert lexical-only, embedding-only, and hybrid behavior from generated config variants
  </acceptance_criteria>
  <behavior>
    - Lexical-only mode remains the current stable path.
    - Embedding-only mode can retrieve from vector-sidecar embeddings.
    - Hybrid mode dedupes merged candidates on record identity and preserves lexical-first explanation behavior.
  </behavior>
  <action>Extend `tests/dual_channel_retrieval.rs` with retrieval-behavior regressions driven by the generated config variants from Plan 09-01. Add `mode_specific_search_behaviors_match_generated_configs`, asserting lexical-only returns lexical results, embedding-only can surface semantic/vector-backed results, and hybrid combines both channels through one `SearchResponse`. Add `hybrid_search_merges_lexical_and_embedding_candidates_by_record_identity`, asserting that when lexical and embedding both surface the same record, the final result set dedupes on record identity rather than duplicating the record. If needed, extend `tests/lexical_search.rs` with a guard assertion that lexical-only behavior remains unchanged under the new mode dispatch.</action>
  <verify>
    <automated>cargo test --test dual_channel_retrieval hybrid_search_merges_lexical_and_embedding_candidates_by_record_identity -- --nocapture</automated>
  </verify>
  <done>A failing retrieval-behavior harness exists that defines the live lexical-only / embedding-only / hybrid contract before production changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement embedding recall and lexical-first fusion inside SearchService</name>
  <files>src/search/mod.rs, src/search/lexical.rs, src/search/score.rs, src/search/rerank.rs, src/memory/repository.rs, tests/dual_channel_retrieval.rs, tests/lexical_search.rs</files>
  <read_first>
    - src/search/mod.rs
    - src/search/lexical.rs
    - src/search/score.rs
    - src/search/rerank.rs
    - src/memory/repository.rs
    - tests/dual_channel_retrieval.rs
    - .planning/phases/09-dual-channel-retrieval-fusion/09-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - `SearchService::search(...)` can dispatch lexical-only, embedding-only, and hybrid retrieval flows
    - hybrid candidate merge dedupes by record identity before final result shaping
    - lexical explanation surfaces remain intact in final `SearchResult` output
    - `cargo test --test dual_channel_retrieval -- --nocapture` exits 0
    - `cargo test --test lexical_search -- --nocapture` remains green
  </acceptance_criteria>
  <behavior>
    - One ordinary retrieval service seam remains in place.
    - Lexical-first semantics remain intact.
    - Embedding-only and hybrid behavior are enabled only through the generated config/runtime mode contract.
  </behavior>
  <action>Extend `src/search/mod.rs` so `SearchService::search(...)` can run lexical-only, embedding-only, or hybrid flows based on the new config-derived retrieval mode contract from Plan 09-01. Reuse `src/search/lexical.rs` as the lexical baseline path, add an embedding recall path that reads persisted `record_embeddings` rows from `src/memory/repository.rs`, and merge lexical/embedding candidates on `record_id` before final ranking in hybrid mode. Keep lexical snippets/citations as the primary explanation source when both channels contribute. Update `src/search/score.rs` and/or `src/search/rerank.rs` only as needed to preserve a stable final `SearchResponse` / `SearchResult` contract while supporting fused candidates. Update the new tests so lexical-only, embedding-only, and hybrid retrieval all pass from generated config variants.</action>
  <verify>
    <automated>cargo test --test dual_channel_retrieval -- --nocapture</automated>
  </verify>
  <done>Ordinary retrieval now supports dual-channel behavior through one shared service while preserving lexical-first explanation semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated retrieval mode config -> SearchService dispatch | Wrong mode dispatch could silently test or run the wrong retrieval path. |
| lexical and embedding candidate sets -> final merged result set | Deduplication and explanation behavior must remain stable when channels overlap. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02 | T | `src/search/mod.rs` / `src/search/rerank.rs` | mitigate | Merge lexical and embedding candidates through one typed service path and dedupe by record identity before final result shaping. |
</threat_model>

<verification>
- `cargo test --test dual_channel_retrieval mode_specific_search_behaviors_match_generated_configs -- --nocapture`
- `cargo test --test dual_channel_retrieval hybrid_search_merges_lexical_and_embedding_candidates_by_record_identity -- --nocapture`
- `cargo test --test dual_channel_retrieval -- --nocapture`
- `cargo test --test lexical_search -- --nocapture`
- `cargo clippy --all-targets -- -D warnings`
</verification>

<success_criteria>
- SearchService supports lexical-only, embedding-only, and hybrid modes.
- Hybrid dedupe/fusion preserves lexical-first explanation behavior.
- The generated config matrix drives the retrieval behavior tests.
</success_criteria>

<output>
After completion, create `.planning/phases/09-dual-channel-retrieval-fusion/09-02-SUMMARY.md`
</output>
