---
phase: 09-dual-channel-retrieval-fusion
plan: 03
type: execute
wave: 3
depends_on:
  - 09-01
  - 09-02
files_modified:
  - src/search/score.rs
  - src/search/rerank.rs
  - src/search/mod.rs
  - tests/dual_channel_retrieval.rs
  - tests/retrieval_cli.rs
autonomous: true
requirements:
  - DCR-04
  - DCR-02
  - OPS-03
must_haves:
  truths:
    - "Final search results can explain whether lexical, embedding, or both channels contributed."
    - "Dual-channel contribution is visible without replacing lexical citation/provenance as the main explanation surface."
    - "Agent-search compatibility with ordinary retrieval remains intact after dual-channel trace extensions."
  artifacts:
    - path: "src/search/rerank.rs"
      provides: "additive dual-channel contribution trace in final result shaping"
    - path: "tests/dual_channel_retrieval.rs"
      provides: "trace-level regression coverage for channel contribution"
    - path: "tests/retrieval_cli.rs"
      provides: "compatibility guard for the ordinary retrieval contract after trace/rerank changes"
  key_links:
    - from: "src/search/rerank.rs"
      to: "src/search/mod.rs"
      via: "final SearchResult trace fields remain compatible with the ordinary retrieval contract"
      pattern: "SearchResult"
    - from: "src/search/mod.rs"
      to: "agent-search consumers"
      via: "ordinary retrieval service remains the shared seam after trace extensions"
      pattern: "SearchService"
---

<objective>
Finish Phase 9 by making dual-channel contribution explainable in the final result trace while preserving lexical citation/provenance as the primary explanation surface.

Purpose: turn dual-channel retrieval from “it works” into “it is operator-readable and safely reusable by higher layers.”
Output: additive trace/rerank extensions and compatibility regressions for ordinary retrieval consumers.
</objective>

<execution_context>
@/home/tongyuan/.codex/get-shit-done/workflows/execute-plan.md
@/home/tongyuan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-dual-channel-retrieval-fusion/09-CONTEXT.md
@.planning/phases/09-dual-channel-retrieval-fusion/09-RESEARCH.md
@.planning/phases/09-dual-channel-retrieval-fusion/09-PATTERNS.md
@src/search/mod.rs
@src/search/score.rs
@src/search/rerank.rs
@tests/dual_channel_retrieval.rs
@tests/retrieval_cli.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add trace/rerank regressions for dual-channel contribution visibility</name>
  <files>tests/dual_channel_retrieval.rs, tests/retrieval_cli.rs</files>
  <read_first>
    - tests/dual_channel_retrieval.rs
    - tests/retrieval_cli.rs
    - src/search/rerank.rs
    - src/search/score.rs
  </read_first>
  <acceptance_criteria>
    - `tests/dual_channel_retrieval.rs` defines `result_trace_reports_channel_contribution`
    - `tests/retrieval_cli.rs` or the dual-channel harness asserts final result traces can distinguish lexical-only, embedding-only, and dual-channel contribution
    - `cargo test --test dual_channel_retrieval result_trace_reports_channel_contribution -- --nocapture` fails before production changes and passes after Task 2
  </acceptance_criteria>
  <behavior>
    - Final result trace must remain operator-readable.
    - Lexical citation/provenance remains the primary explanation surface.
    - Higher-layer consumers should still be able to consume the ordinary retrieval result contract.
  </behavior>
  <action>Extend `tests/dual_channel_retrieval.rs` with `result_trace_reports_channel_contribution`, asserting the final result trace can distinguish lexical-only, embedding-only, and both-channels contribution for each result. If needed, add a retrieval CLI compatibility assertion that the enriched trace remains valid for existing ordinary retrieval consumers. Keep the tests focused on trace/rerank semantics, not on raw recall behavior already covered in Plan 09-02.</action>
  <verify>
    <automated>cargo test --test dual_channel_retrieval result_trace_reports_channel_contribution -- --nocapture</automated>
  </verify>
  <done>A failing trace-level regression exists that defines the final explainability contract for dual-channel retrieval.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend rerank/result traces to explain lexical vs embedding contribution without breaking consumers</name>
  <files>src/search/score.rs, src/search/rerank.rs, src/search/mod.rs, tests/dual_channel_retrieval.rs, tests/retrieval_cli.rs</files>
  <read_first>
    - src/search/mod.rs
    - src/search/score.rs
    - src/search/rerank.rs
    - tests/dual_channel_retrieval.rs
    - tests/retrieval_cli.rs
    - .planning/phases/09-dual-channel-retrieval-fusion/09-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - final `SearchResult` trace can indicate lexical-only, embedding-only, or both-channel contribution
    - lexical citation/provenance fields remain intact for final results
    - `cargo test --test dual_channel_retrieval -- --nocapture` exits 0
    - `cargo test --test retrieval_cli -- --nocapture` exits 0
    - `cargo clippy --all-targets -- -D warnings` exits 0
  </acceptance_criteria>
  <behavior>
    - Explainability is additive, not a rewrite of current lexical result contracts.
    - Lexical remains the main explanation source even when embedding contributes.
    - Ordinary retrieval remains a shared seam for higher-level consumers such as agent-search.
  </behavior>
  <action>In `src/search/rerank.rs` and any supporting `src/search/score.rs` / `src/search/mod.rs` structures, add additive trace fields that can report channel contribution for each final result: lexical-only, embedding-only, or both. Keep current citation and authority-row provenance intact so lexical explanation surfaces remain primary. Update the new trace regressions and any necessary retrieval CLI compatibility assertions so the enriched result contract remains stable and operator-readable after dual-channel behavior lands.</action>
  <verify>
    <automated>cargo test --test dual_channel_retrieval -- --nocapture</automated>
  </verify>
  <done>Dual-channel retrieval now explains channel contribution clearly without breaking the ordinary retrieval contract used by higher layers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fused result set -> operator/debug interpretation | Channel contribution must stay transparent after fusion. |
| enriched retrieval trace -> higher-layer consumers | Trace extensions must not break ordinary retrieval consumers such as agent-search. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | R | `src/search/rerank.rs` / `src/search/mod.rs` | mitigate | Make channel contribution explicit in final traces and test compatibility with ordinary retrieval consumers. |
</threat_model>

<verification>
- `cargo test --test dual_channel_retrieval result_trace_reports_channel_contribution -- --nocapture`
- `cargo test --test dual_channel_retrieval -- --nocapture`
- `cargo test --test retrieval_cli -- --nocapture`
- `cargo clippy --all-targets -- -D warnings`
</verification>

<success_criteria>
- Final traces explain lexical vs embedding contribution.
- Lexical citations remain the main explanation surface.
- Ordinary retrieval consumers remain compatible after the trace extension.
</success_criteria>

<output>
After completion, create `.planning/phases/09-dual-channel-retrieval-fusion/09-03-SUMMARY.md`
</output>
