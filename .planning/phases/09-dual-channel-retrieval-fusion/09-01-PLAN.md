---
phase: 09-dual-channel-retrieval-fusion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/config.rs
  - tests/dual_channel_retrieval.rs
autonomous: true
requirements:
  - DCR-01
  - DCR-04
must_haves:
  truths:
    - "The codebase can parse retrieval-relevant fields from the existing root `config.toml`."
    - "A shared parsed config base can generate lexical-only, embedding-only, and hybrid retrieval variants."
    - "Dual-channel retrieval tests use generated config variants instead of unrelated hardcoded fixtures."
  artifacts:
    - path: "src/core/config.rs"
      provides: "retrieval-relevant config parsing / derivation support for dual-channel mode tests"
    - path: "tests/dual_channel_retrieval.rs"
      provides: "mode-matrix regression harness derived from shared config parsing"
  key_links:
    - from: "config.toml"
      to: "src/core/config.rs"
      via: "retrieval-relevant fields parsed into test/runtime variants"
      pattern: "config parser"
    - from: "src/core/config.rs"
      to: "tests/dual_channel_retrieval.rs"
      via: "generated lexical-only / embedding-only / hybrid configs"
      pattern: "mode matrix"
---

<objective>
Add retrieval-relevant config parsing and generated mode variants so Phase 9 can be tested through one shared config contract instead of hand-maintained ad hoc fixtures.

Purpose: make the user-supplied `config.toml` part of the dual-channel retrieval contract and use it to drive the lexical-only / vector-only / hybrid test matrix.
Output: config-derived mode matrix plus focused retrieval-mode tests.
</objective>

<execution_context>
@/home/tongyuan/.codex/get-shit-done/workflows/execute-plan.md
@/home/tongyuan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-dual-channel-retrieval-fusion/09-CONTEXT.md
@.planning/phases/09-dual-channel-retrieval-fusion/09-RESEARCH.md
@.planning/phases/09-dual-channel-retrieval-fusion/09-PATTERNS.md
@config.toml
@src/core/config.rs
@tests/retrieval_cli.rs
@tests/lexical_search.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add config-derived mode-matrix regressions for lexical-only, embedding-only, and hybrid retrieval</name>
  <files>tests/dual_channel_retrieval.rs</files>
  <read_first>
    - config.toml
    - src/core/config.rs
    - tests/retrieval_cli.rs
    - tests/lexical_search.rs
  </read_first>
  <acceptance_criteria>
    - `tests/dual_channel_retrieval.rs` defines `parses_root_config_into_dual_channel_variants`
    - `tests/dual_channel_retrieval.rs` defines `generated_mode_matrix_covers_lexical_embedding_and_hybrid`
    - `cargo test --test dual_channel_retrieval parses_root_config_into_dual_channel_variants -- --nocapture` fails before production changes and passes after Task 2
    - the new tests derive three explicit retrieval modes from a shared parsed config base instead of duplicating three unrelated fixture files
  </acceptance_criteria>
  <behavior>
    - Config parsing must cover the retrieval-relevant subset of the existing root config contract.
    - Tests must explicitly cover lightweight-only, vector-only, and hybrid retrieval modes.
    - The mode matrix itself is a required behavior, not only a test convenience.
  </behavior>
  <action>Create `tests/dual_channel_retrieval.rs` as a new integration-focused harness. Parse the existing repo-root `config.toml` retrieval-relevant fields (embedding + vector sections and any needed retrieval mode override) into one shared base config, then derive three runtime variants: lexical-only, embedding-only, and hybrid. Add `parses_root_config_into_dual_channel_variants`, asserting the parsed base and derived variants preserve the relevant backend/model/vector configuration. Add `generated_mode_matrix_covers_lexical_embedding_and_hybrid`, asserting the three derived variants exist and encode distinct retrieval modes rather than ad hoc hardcoded fixtures.</action>
  <verify>
    <automated>cargo test --test dual_channel_retrieval parses_root_config_into_dual_channel_variants -- --nocapture</automated>
  </verify>
  <done>A failing config-derived mode-matrix regression harness exists that defines how Phase 9 should consume the existing `config.toml` for retrieval-mode testing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend retrieval config parsing to support generated dual-channel mode variants</name>
  <files>src/core/config.rs, tests/dual_channel_retrieval.rs</files>
  <read_first>
    - src/core/config.rs
    - config.toml
    - tests/dual_channel_retrieval.rs
    - .planning/phases/09-dual-channel-retrieval-fusion/09-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - `src/core/config.rs` can parse the retrieval-relevant subset of the root `config.toml`
    - the codebase exposes a way to derive lexical-only, embedding-only, and hybrid runtime variants from a shared parsed config base
    - `cargo test --test dual_channel_retrieval -- --nocapture` exits 0
    - lexical-only defaults and existing config tests remain green
  </acceptance_criteria>
  <behavior>
    - Parsing should be retrieval-focused and additive, not a full unrelated app-config rewrite.
    - The generated mode variants must preserve backend/model/vector settings where applicable.
    - Existing lexical-only defaults must remain valid.
  </behavior>
  <action>In `src/core/config.rs`, add a retrieval-focused parser/adapter for the existing root `config.toml` contract so the retrieval-relevant subset of `[embedding]` and `[vector]` can be read into a typed base config. Add an additive helper/API that can derive lexical-only, embedding-only, and hybrid runtime variants from that parsed base while preserving the current lexical-only defaults for ordinary runtime config loading. Update `tests/dual_channel_retrieval.rs` so the new parsing and variant-generation tests pass and verify that the derived modes keep the intended backend/model/vector substrate fields intact.</action>
  <verify>
    <automated>cargo test --test dual_channel_retrieval -- --nocapture</automated>
  </verify>
  <done>The codebase can derive the three retrieval-mode test configurations from the real config schema instead of maintaining disconnected test fixtures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| root config contract -> retrieval mode matrix | User-facing config shape now drives retrieval behavior selection in tests and later runtime paths. |
| config parsing -> dual-channel behavior | Misparsed config could silently test or run the wrong retrieval mode. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | R | `src/core/config.rs` / `tests/dual_channel_retrieval.rs` | mitigate | Parse the real config shape and assert generated lexical-only / embedding-only / hybrid variants explicitly, so mode selection cannot drift silently. |
</threat_model>

<verification>
- `cargo test --test dual_channel_retrieval parses_root_config_into_dual_channel_variants -- --nocapture`
- `cargo test --test dual_channel_retrieval generated_mode_matrix_covers_lexical_embedding_and_hybrid -- --nocapture`
- `cargo test --test dual_channel_retrieval -- --nocapture`
- `cargo clippy --all-targets -- -D warnings`
</verification>

<success_criteria>
- Retrieval-relevant config parsing supports the real root config contract.
- Three generated mode variants exist for lexical-only, embedding-only, and hybrid retrieval.
- Dual-channel tests no longer rely on disconnected ad hoc config fixtures.
</success_criteria>

<output>
After completion, create `.planning/phases/09-dual-channel-retrieval-fusion/09-01-SUMMARY.md`
</output>
